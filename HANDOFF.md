# Handoff: KickbaseAgent Dashboard

**Generated**: 2026-07-29, zuletzt komplett überarbeitet 2026-08-02 (7 Feedback-Quick-Wins per subagent-driven-development umgesetzt — siehe unten)
**Branch**: main
**Status (2026-08-02)**: `main` und `origin/main` sind **synchron** — nichts Ungepushtes. 7 Quick-Win-Items aus `feedback/current` sind umgesetzt, gemerged und gepusht (siehe Session 2026-08-02 unten). Von den ursprünglich 14 dokumentierten offenen Items sind jetzt noch 3 Need-Planning + 2 Backlog + 2 bewusst zurückgestellte (Wunschkader-Direkt-Speichern, Transfermarkt-Tausendertrennzeichen — beide ambig, brauchen Produktentscheidung) offen, plus 1 neues Item seit dem letzten Audit (Umlaute/Apostrophe-Suche — dieses ist inzwischen bereits umgesetzt, siehe unten). **Firestore-Status-Updates auf `"done"` für alle 8 in dieser Session erledigten Items (7 Quick-Wins + Heavy-Cron-Verifikation `2464b2e6`) stehen noch aus** — die Firestore-Schreib-Aktion wurde vom Sandbox-Permission-Classifier blockiert (Schreibzugriff auf Produktions-Firestore), siehe Warnings.

## Session 2026-08-02 (neu, oben angehängt)

**Kontext:** User-Auftrag: "Fixe alle Quick wins nacheinander, bei denen du keinen Input von mir brauchst, hole dir vorher den Skill aus superpowers und arbeite nach TDD." Vorgehen: `superpowers:subagent-driven-development` + `superpowers:writing-plans`, durchgängig subagent-driven (Standing-Preference, siehe [[feedback_always_subagent_driven]]).

**Heavy-Cron-Verifikation** (`2464b2e6`, kein Code-Fix, reine Ops-Prüfung): einziger Schedule-Lauf seit dem Timing-Fix vom 2026-08-01 (`b53b131`) landete um 20:09 UTC (Ziel 19:04 UTC + gemessener ~65min-Delay = ~22:09 Berlin, passt zum Ziel ~22:07 Berlin), Guard-Step griff nicht fälschlich. Nur 1 Datenpunkt, aber passt zur Erwartung — laut Handoff-eigenem Schließungskriterium als erledigt behandelt.

**Vorarbeit:** `.gitattributes` (`* text=auto eol=lf`) ergänzt (Commit `65e5983`) — behob CRLF-Drift auf 19 Dateien im Haupt-Checkout (Windows-Seite hatte sie mit CRLF überschrieben, git-Objekte waren immer LF; reines Zeilenenden-Rauschen, kein Inhaltsunterschied) + 5 `MDs/*.md`-Dateien, die tatsächlich MIT CRLF committed waren, auf LF renormalisiert.

**Investigation vor dem Plan:** Fork-Agent gegen den echten Code + `feedback/current` (Firestore) verifiziert alle 10 ursprünglich kategorisierten Quick-Win-Items. Zwei stellten sich als echt ambig heraus und wurden bewusst NICHT umgesetzt (User-Entscheidung nötig, nicht geraten):
- **Wunschkader-Direkt-Speichern** (`f462d415`): Feedback-Text ("vielleicht doch direkt speichern") liest sich wie Wunsch nach echtem Auto-Save, könnte aber auch nur "Button erreichbarer machen" bedeuten — deutlich unterschiedlicher Implementierungsaufwand.
- **Tausendertrennzeichen-Filter** (`d390f441`): Feedback nennt "im Transfermarkt", aber der einzige Marktwert-Zahlenfilter mit dem beschriebenen Lesbarkeits-Problem liegt tatsächlich in "Alle Spieler" — Transfermarkt-Tab hat gar keinen solchen Filter. Unklar ob User den Tab wörtlich meint oder das Konzept.
- Zusätzlich **1 neues Item seit dem letzten Audit gefunden** (`8ca0f3d9`, Umlaute/Apostrophe-insensitive Spielersuche) — als Quick Win eingestuft und mit umgesetzt.

**Plan:** `docs/superpowers/plans/2026-08-02-feedback-quick-wins.md` (7 Tasks, committed+gepusht vor Implementierungsstart per [[feedback_specs_plans_straight_to_main]]). Frontend hatte zuvor **keinen Test-Runner** — Plan führt `vitest` (node-Environment, kein jsdom) für die 2 Tasks mit echter testbarer Logik ein; die anderen 5 (reine Text-/Layout-Änderungen) laufen nur gegen `npm run typecheck`+`npm run build`, keine Fake-Tests.

**Umgesetzt (7 Tasks, je eigener Implementer+Reviewer, alle Reviews clean, teils nach Fix-Runde):**
1. **"(Standard)"-Zusatz bei Sortier-Label entfernen** (SpekulationTab/TransfermarktTab) — Commit `f2f48d9`.
2. **Feedback-Formular-Zeichenlimit 1000→2000** (FeedbackTab, beide Textareas) — Commit `07d72e5`. *(Task-Review fand: Verifikation nur `typecheck`, nicht `npm run build` gegen Global Constraints geprüft — nachträglich vom Controller selbst per direktem `npm run build`-Lauf verifiziert, deckt auch denselben Lücken-Fund bei Task 1 rückwirkend ab.)*
3. **"Marktwert-Update"-Hinweiszeile** in Spekulation+Transfermarkt — Commit `2be6c37`, im finalen Whole-Branch-Review nochmal überarbeitet (siehe unten).
4. **Mobile Tab-Überschrift** (App.tsx, `sm:hidden`, aus bestehendem `TABS`-Label) — Commit `90a4476`.
5. **Kopf-an-Kopf-Kacheln 1T/3T auf Desktop zusammengelegt** (MlGenauigkeitTab, `lg:grid-cols-2`) — Commit `c9ce4b7`.
6. **Umlaute/Apostrophe-insensitive Spielersuche** (TDD): neue `normalizeSearchText()` in `derive.ts` (NFD-Normalize + `\p{Diacritic}`-Strip + Apostroph-Varianten-Strip + lowercase), an allen 6 Spieler-Namens-Suchstellen angewendet (`PlayerNamePicker`, `AlleSpielerTab`, `SpekulationTab`, `TransfermarktTab`, `WunschkaderTab` ×2) — `LigaanalyseTab`s Manager-Suche bewusst NICHT angefasst (Feedback war explizit "Spielersuche"). Führt `vitest` als erste Test-Infrastruktur des Frontends ein. Commit `ac0b994`.
7. **Mobiler Header versteckt sich beim Runterscrollen** (TDD): reine Funktion `nextHeaderVisible()` + Hook `useHideOnScroll()` (`frontend/src/lib/useHideOnScroll.ts`, neu), verdrahtet in `App.tsx`s `<header>` (`sticky top-0` + Tailwind-Transform-Toggle). Commit `74b44be`.

**Finaler Whole-Branch-Review (Opus, nach allen 7 Tasks) fand 2 Important + 5 Minor, EIN Fix-Dispatch + eine Scoped-Re-Review, alle behoben (Commit `ce7583d`):**
- **Important**: Header wurde durch Task 7 versehentlich auch auf Desktop `sticky` (Plan/Feedback wollten explizit nur mobil) — `sm:static` ergänzt, stellt exaktes Vorher-Verhalten auf Desktop wieder her.
- **Important**: die neue "Marktwert-Update"-Zeile (Task 3) war ein inhaltsleeres Fragment, das den Satz direkt darüber wörtlich wiederholte (beide Tabs erwähnen im bestehenden Hinweistext schon "22-Uhr-Marktwert-Update") — zu einem eigenständigen Satz umformuliert. *(Reviewer-Hypothese, NICHT umgesetzt: die Zeile könnte bei Kickbase eigentlich ein Trenner INNERHALB der Auktionsliste sein, nicht eine Fußzeile — unklar, wäre größere Änderung, siehe Not Yet Done.)*
- **Minor** (alle mit umgesetzt): zweites `<h1>` auf Mobile → `<h2>`; MlGenauigkeitTab-Grid liess bei fehlenden 3T-Metriken (aktuell live der Fall) eine leere Spalte → jetzt konditional; `useHideOnScroll`s `lastY`-Ref jetzt beim Mount aus echtem `window.scrollY` geseedet statt `0`; CI (`frontend-pilot.yml`) führt jetzt `typecheck`+`test` vor dem Build aus (bisher lief der neue Test-Suite nirgends automatisch); `vitest.config.ts` in `vite.config.ts` konsolidiert (verhinderte vorher, dass Tests den `@vitejs/plugin-react`/`base`-Pfad bekommen — aktuell folgenlos, da nur pure-function-Tests existieren, aber Falle für den ersten `.tsx`-Test).
- **Bewusst NICHT gefixt, geparkt**: `normalizeSearchText()` deckt nicht-zerlegbare lateinische Buchstaben (ø/ł/đ/æ/ß) nicht ab (z.B. "Odegaard" findet "Ødegaard" nicht) — real, aber keine Regression, geringe Praxisrelevanz für aktuellen Bundesliga-Kader. Header-vs-Modal-z-Index-Kollision (beide `z-10`, aktuell nur zufällig korrekt durch DOM-Reihenfolge) — real, aber aktuell unsichtbar, Fix würde 8 Modal-Dateien anfassen für ein Nicht-Problem.

Alle 7 Tasks + Fix-Wave: Frontend 8/8 Tests, `tsc --noEmit` 0 Fehler, `npm run build` erfolgreich. Backend 226/226 Tests unberührt (reine Frontend-Änderung, zur Sicherheit trotzdem vor dem finalen Merge nochmal komplett gegengelaufen). Gemerged + gepusht (`123412f..ce7583d`).

## Session 2026-08-01 (neu, oben angehängt)

**Umgesetzt + gepusht**: Heavy-Cron (`dashboard-marktwerte.yml`) landete real konsistent nach Mitternacht Berlin (User-Fund über `feedback/current`) — GitHub Actions' `schedule`-Trigger verzögerte 3 gemessene Läufe um 59-66min. Cron-Zeit auf `19:04 UTC` vorverlegt (kompensiert die gemessene Verzögerung, Ziel ~22:07 Berlin) + neuer Guard-Step, der einen zu frühen Lauf (12-21 Uhr Berlin) hart abbricht statt mit veralteten Marktwerten zu schreiben. Commit `b53b131`, **gepusht**. Muss beim DST-Wechsel im Herbst nochmal angepasst werden (Kommentar im Workflow steht dabei). Verifikation der ersten realen Läufe mit neuer Zeit steht noch aus.

**Drei Spec+Plan-Paare geschrieben und committed, NICHT umgesetzt** (User: "erstmal nur Pläne", jeweils per `superpowers:brainstorming` + `superpowers:writing-plans`, gegen den echten Code verifiziert):

1. **Prognose 1T/3T im Frontend** (Spec `docs/superpowers/specs/2026-08-01-ml-horizonte-frontend-anzeige-design.md`, Plan `docs/superpowers/plans/2026-08-01-ml-horizonte-frontend-anzeige.md`, 7 Tasks): `momentumAssessment()`/"Einschätzung" komplett durch zwei rohe Zahlen ersetzt ("Prognose 1T"/"Prognose 3T" + MAE), in allen 4 Detail-Modals + Karten wo heute schon 1T sichtbar ist + neuer Transfermarkt-Spalte. Modell-Tracking bekommt einen zweiten Kopf-an-Kopf-Block für den 3-Tages-Horizont. 3T-Trend-Pfeil-Einfärbung bewusst noch nicht dabei (eigene Schwellenwerte fehlen noch — kommen als Nebenprodukt von Punkt 2).
2. **3-Tage-Modell eigene Hyperparameter-Suche** (Spec `docs/superpowers/specs/2026-08-01-ml-3d-tuning-design.md`, Plan `docs/superpowers/plans/2026-08-01-ml-3d-tuning.md`) — **abgeschlossen, kein Gewinner** (2026-08-02, 325-Konfigurationen-Suche fand einen Daten-Leck-Artefakt statt echter Verbesserung, siehe Failed Approaches + `docs/superpowers/plans/2026-08-01-ml-3d-tuning-results.md`). Der dafür genutzte Worktree `worktree-ml-3d-tuning` ist gelöscht — alles Wissenswerte (Ergebnis-Dokument, HANDOFF-Einträge, die daraus abgeleitete 3T-Einfärbung) ist auf `main`.
3. **Bug: Wunschkader-Änderung nicht live in Eigenes Team** (Spec `docs/superpowers/specs/2026-08-01-eigenes-team-wunschkader-live-sync-design.md`, Plan `docs/superpowers/plans/2026-08-01-eigenes-team-wunschkader-live-sync.md`, 4 Tasks, User-Fund über `feedback/current`): `EigenesTeamTab` liest den Split aus einer beim App-Mount eingefrorenen Snapshot-Kopie, `WunschkaderTab.handleSave()` aktualisiert nie das App-Level-State. Fix: `wunschkader/current`s Ziel-Liste live per eigenem `getDoc()` gelesen (Fallback auf Snapshot-Kopie bei Cold-Start/Fehler), `onSaved`-Callback synct sofort zurück. **Bewusst NUR `targets`, nicht `formation`** — extra dafür angepasst (Commit `57c8547`), um Kollision mit Punkt 4 zu vermeiden.
4. **Wunschkader: alle 10 erlaubten Formationen + live abgeleitete Positions-Buttons** (Spec `docs/superpowers/specs/2026-08-01-wunschkader-formationen-design.md`, Plan `docs/superpowers/plans/2026-08-01-wunschkader-formationen.md`, 4 Tasks, User-Fund über `feedback/current`): nur 4 von tatsächlich 10 Kickbase-Formationen konfiguriert (Recherche per WebSearch/WebFetch, **Drittquelle DAZN, nicht offizielle Kickbase-Doku** — vor Umsetzung idealerweise gegen den echten In-App-Dialog gegenchecken). Formation nicht mehr per Combobox vorausgewählt, sondern live aus der tatsächlichen Ziel-Belegung abgeleitet (`canAddStarter()`/`matchedFormation()`), `wunschkader_formation` komplett aus Backend+Contract-Test+`types.ts` entfernt.

**Zurückgestellt (User-Entscheidung, nicht anfangen ohne Nachfrage)**: Feedback-Item "Tages-Dashboard mit Handlungsbedarf" (Verkaufen-Kandidaten + Wunschkader-Ziele-auf-dem-Markt gebündelt) — Brainstorming begonnen, User hat nach der ersten Frage abgebrochen: "das bedarf größerer Planung", erst nach den anderen Punkten.

**Von parallelen Agenten/Sessions in derselben Zeit bearbeitet** (NICHT von dieser Session, nicht anfassen/duplizieren — per `git log`/`git worktree list` festgestellt, klassischer Fall von [[project_kickbaseagent_git_workflow]]s "mehrere Agenten parallel"-Warnung):
- Feedback-Item "Transfermarkt/Alle Spieler auch Kartenansicht" → Spec+Plan bereits committed (`2bc5cbd`/`52c0aee`/`7fb9687`), eigene Worktrees `worktree-card-view-toggle`/`worktree-card-view-toggle-plan`.
- Feedback-Item "Wunschkader-Buttons als Icons" → Spec committed (`9f103fb`, "Wunschkader-Aktions-Buttons mit Icons"), eigener LOCKED Worktree `worktree-wunschkader-action-icons` (aktiv in Arbeit).

**Feedback-Item-Status** (`feedback/current`, Firestore) — **ALLE weiterhin `status:"open"`**, nichts wurde in dieser Session auf `"done"` gesetzt (bewusst — nichts wurde implementiert+live-verifiziert, nur geplant). Jeder der 4 zugehörigen Pläne oben hat als letzten Task explizit das Setzen von `status:"done"` nach der Umsetzung vorgesehen.

---

**Status (2026-07-31, unverändert seit dem letzten vollständigen Audit)**: Players-Map-Redesign FERTIG, GEMERGED, GEPUSHT und LIVE VERIFIZIERT (siehe unten, unverändert gültig). Danach: App-weiter Security-Audit (gefixt), ML-Tab-Klarheits-Fixes inkl. `_infer_today()`-Root-Cause-Bug, App-weite Feld-Reihenfolge/Naming-Überarbeitung (Fitness/Verfügbarkeit, Kapital/Budget), zwei Live-Bugs gefixt (fetcher.py-Expiry-Schätzung, bid_premium.py-Pointer-Datenverlust). **Vier Folge-Features umgesetzt, gemerged UND gepusht**: Gebotsvorschläge+Positions-Bedarf, Gebotstracking (bought_by_self/unsold-Erkennung, inkl. Backfill-Bugfix), Ligaanalyse-Detailansicht, Spieler-Vergleichsansicht (`PlayerCompareModal`). Contract-Test für `export()`s Snapshot-Key-Set ergänzt. Drei ML-Verbesserungs-Experimente durchgeführt (HGB-Tuning, Verletzungs-Proxy, relativer Zielwert) — keins hat die Baseline geschlagen, siehe Failed Approaches. **Push-Policy geändert**: Claude darf jetzt selbst auf `main` pushen, wenn vorher alle Tests grün sind (User-Anweisung 2026-07-30, überschreibt die alte "nur lokal committen"-Regel). **2026-07-31 zusätzlich (vollständig, siehe Completed)**: Wunschkader-UX-Fixes + Cron-Verschiebung, echter Kaufpreis (`purchase_price`/`mvgl`), Fitness-Historie-Sammelpipeline+ML-Integration, komplettes Icon/Crest-System, ML-Horizonte (3-Tage-Prognose+Konfidenz-Signal, `sellSignal()`-Umbau), Feedback-Tab "Bugs & Ideen" (**neuer, bis zu diesem Update in diesem Dokument nie erwähnter zweiter Eingangskanal für Bugs/Ideen, siehe Warnings**), Mobile-Swipe-Gesten, Tab-Persistenz per `localStorage`, `backfill_prediction_log()`-Generalisierung auf beide Horizonte + Live-90-Tage-Lauf gegen Produktion.

**Korrektur zu `worktree-wunschkader-fixes`**: entgegen mehrerer früherer Einträge in diesem Dokument war das NIE eine fremde, andere Session — es ist dieselbe Session, die auch die vier Folge-Features umgesetzt hat (der Kontext lief über eine Zusammenfassung hinweg weiter, ein früherer Zwischenstand dieses HANDOFFs hat den Worktree deshalb fälschlich als "andere aktive Session, nicht anfassen" dokumentiert). Beide Commits (`e6eaef2` Bugfixes + der `4663dcf`/`a719204`-Compare-Vorlauf) sind von dieser Session, inzwischen vollständig in `main` gemerged. Der Worktree enthielt zusätzlich `docs/superpowers/plans/2026-07-30-playwright-component-testing.md` (Playwright Component Testing für `WunschkaderTab`, per User-Wunsch nach den beiden live gefundenen Bugs, `superpowers:writing-plans`-Format, 5 Tasks inkl. Mutation-Checks gegen die alten Buggy-Commits) — **jetzt ebenfalls gemerged**, aber NOCH NICHT umgesetzt (User hat sich noch nicht zwischen Subagent-Driven/Inline entschieden).

## Goal

KickbaseAgent-Dashboard von 5 parallelen, namensverknüpften Firestore-Arrays auf eine einzige `players`-Map (`player_id -> Rohdaten`) plus dünne Referenzlisten umstellen, und alle ableitbaren Berechnungen (Signal/Fairwert/Status-Text/Trend/Budget/ROI/Hype-Gipfel/Auktions-Countdown) vom Backend ins Frontend verschieben (`frontend/src/lib/derive.ts`, neu). Ziel: kleineres Firestore-Dokument, `player_id` statt Name als einziger Join-Key, keine serverseitige Ableitungslogik mehr duplizieren.

## Completed

- [x] **Alle 7 ursprünglichen Tabs auf neues React-Frontend migriert** (Eigenes Team, Spekulation, Wunschkader, Transfermarkt, Ligaanalyse, Alle Spieler, ML-Genauigkeit/"Modell-Tracking") — Commits `1fd2e20` … `a57b549` u.a. **Inzwischen ein 8. Tab dazugekommen** ("Bugs & Ideen", siehe weiter unten) — `App.tsx`s `TABS`/`ACTIVE_TABS` hat aktuell 8 Einträge, nicht mehr 7.
- [x] **Cutover durchgeführt**: neues Frontend ist Standard-UI auf GitHub Pages, alte `index.html` lief zuletzt unter `/old/` (Commit `a57b549`). Zwei operative Blocker dabei gefixt: `dashboard-marktwerte.yml` war nie manuell angestoßen worden (`gh workflow run ...`), und GitHub Pages `build_type` stand auf `"legacy"` statt `"workflow"` (`gh api -X PUT repos/TyraBite/KickbaseAgent/pages -f build_type=workflow`).
- [x] **Cron-Split**: `DASHBOARD_MODE=light|heavy` — `dashboard.yml` läuft weiter alle 2h (Transfermarkt/Ligaanalyse-relevante Daten), `dashboard-marktwerte.yml` (neu) läuft 1×/Tag um 22:10 Berlin-Zeit (Marktwerte/ML-Prognosen — teure Kickbase-Calls). Selbstheilung über `_resolve_is_light()`/`_resolve_heavy_data()` in `src/dashboard_export.py`.
- [x] **Firestore-Write-Fehler sind jetzt sichtbar**: ein fehlgeschlagener Firestore-Write lässt `dashboard.yml` failen, statt stillschweigend die alte Seite stehen zu lassen (`FirestoreWriteError` in `src/firestore_db.py`).
- [x] **6 tote Firestore-Collections entfernt** (waren nie gelesen — verifiziert per Explore-Agent).
- [x] **`status_label`-Mapping final verifiziert** (Commits `8b8af6a` → `81ed8e3` → `fa9f282`): `{1: "Verletzt", 2: "Angeschlagen", 4: "Im Aufbau"}`, jeweils per echtem In-App-Check an konkreten Spielern bestätigt (Ben Seghir, Matsima, Hollerbach) — siehe `MDs/codes.md` für den vollen Korrektur-Verlauf. TDD für jede Korrektur genutzt.
- [x] **UI-Polish nach Feld-Audit**: Eigenes-Team-Kacheln entschlackt (Status als Badge statt Zeile, ML-Prognose farbig mit Pfeil, Markt-Badge auf Watchlist, Detailansicht ergänzt) — Commit `1bcba9e`. Dark-Mode-Tabellen-Textfarbe gefixt. Spalten-Klick-Sortierung zu allen Tabellen ergänzt (`table.tsx`).
- [x] **Wunschkader-Budgetplanung korrigiert** (Commit `43c4854`, diese Session): Cash nutzt jetzt `own_available_budget` statt `own_budget_exact`; Verkaufserlöse werden automatisch aus eigenem Kader minus aktuellen Wunschkader-Zielen abgeleitet statt aus einer unabhängigen manuellen Liste; Eingeplant ist jetzt reiner Marktwert mit echtem laufendem Transfermarkt-Gebot (`is_own_leading_bid`/`leading_bid_price`) als Override statt manuellem `actual_bid`-Feld + 10%-Schätzung; "Rest" → "Spielraum" umbenannt.
- [x] **Großes Redesign vollständig durchgeplant und dokumentiert** (nicht implementiert, siehe Not Yet Done): `docs/superpowers/plans/2026-07-29-players-map-datenstruktur.md`, 21 Tasks im `writing-plans`-Format, mit vollständigem Code für die kritischen Teile.
- [x] **Bug: Wunschkader-Cash zeigte 131 Mio. statt echter 68 Mio.** (Commit `fe40a39`): `own_available_budget` (Kontostand + 33% Überziehungsrahmen, `src/manager_budgets.py::_overdraft()`) war die falsche Quelle für die Cash-Zeile — zurück auf `own_budget_exact` (echter Kontostand). Live vom User verifiziert ("passt so").
- [x] **Bug: Transfermarkt/Spekulation im Light-Cron bis zu 24h veraltet** (Commit `1923c05`, per `systematic-debugging`): `export()` übernahm `transfermarkt`/`spekulation` im Light-Modus unverändert aus dem letzten Firestore-Snapshot statt aus den frisch gefetchten `market_listings` neu zu bauen — neu gelistete Spieler (Beispiel: Hajdari) blieben bis zum nächsten Heavy-Lauf unsichtbar. Root Cause live bestätigt (echter API-Call zeigte Hajdari, Firestore-Snapshot nicht), Fix per TDD (Test reproduziert den Bug, dann Fix, Test grün), gepusht, `dashboard.yml` zweimal manuell angestoßen (`gh workflow run`) — zweiter Lauf (nach dem Push!) zeigte Hajdari live im Snapshot. 84/84 Backend-Tests grün.
- [x] **Spec: Gebotsvorschläge für Kickbase-Systemangebote + Positions-Bedarfs-Analyse** (Commits `6a77fb5`, `26f13cd`): `docs/superpowers/specs/2026-07-29-gebotsvorschlaege-design.md`, per `superpowers:brainstorming` durchgesprochen. Historische Aufschlags-Perzentile (Ähnlichkeits-gewichtet, wie `scoreReplacementPool()`) aus den ~104 bisherigen Systemkäufen dieser Liga (Activity-Feed, `get_market_value_history()`-Backfill) plus ein league-weiter Deckungsgrad pro Position (Stammspieler im Kader ÷ echte Startelf-Zahl dieser Position, aus bereits abgerufenen `get_manager_squad()`-Daten, keine neuen API-Calls). Umsetzungs-Sperre durch den jetzt erfolgten Merge aufgehoben.
- [x] **Kleine Fixes während der Wartezeit erledigt**:
  - **Bug: `prompt_builder.py` Kosten/Punkt** (Commit `72619f0`): `_cost_per_point()` bekam bei beiden Aufrufstellen `total_points` (Saison-Summe) statt `average_points` (Punkteschnitt) übergeben — inkonsistent zur sonst überall etablierten Definition (`market_value/average_points`, siehe `_k_per_point`). Per TDD gefixt, neue Testdatei `tests/test_prompt_builder.py` (gab's bisher gar nicht für dieses Modul).
  - **`frontend/src/vite-env.d.ts` ergänzt + `typecheck`-Script** (Commit `34eb8ec`): behebt den letzten verbleibenden `tsc`-Fehler (`ImportMeta.env`), `npm run typecheck` kapselt den bisher nur manuell ausgeführten Befehl. `tsc` läuft jetzt mit 0 Fehlern.
- [x] **Bug: Ligaanalyse-Budget für Fleischmanns zeigte 333 Mio. statt echter ~45 Mio.** (Commit `eec9957`, per `systematic-debugging`, live reproduziert und live verifiziert): `KICKBASE_LEAGUE_START_DATE` existierte nur in der lokalen `.env`, war in KEINER GitHub-Actions-Workflow-Env verdrahtet (`KICKBASE_LEAGUE_START_BUDGET` war schon korrekt als `'80000000'` hardcoded in beiden Workflows). `_parse_trades()` lief in Produktion deshalb immer ohne Datums-Filter und zählte 16 Trades von vor Saisonstart (März 2026) mit — bei Fleischmanns als aktivstem Trader am sichtbarsten (35 statt 19 Trades). Fix: `gh secret set KICKBASE_LEAGUE_START_DATE` (Wert aus lokaler `.env`, nie im Klartext ausgegeben) + `KICKBASE_LEAGUE_START_DATE: ${{ secrets.KICKBASE_LEAGUE_START_DATE }}` in `dashboard.yml` UND `dashboard-marktwerte.yml` ergänzt. Betraf potenziell alle Nicht-eigenen Manager-Budgets in der Ligaanalyse, nicht nur Fleischmanns.
- [x] **Bug: Light-Cron setzte ML-Prognosen bei jedem Lauf auf None zurück** (Commit `b14c961`, per `systematic-debugging`, per Vollreproduktion von `export()` bestätigt): `_resolve_heavy_data()`s Light-Zweig rekonstruierte Predictions aus `alle_spieler`s `ml_prediction`-Feld, das `_build_alle_spieler()` nie setzte. **Im Merge-Konflikt mit dem players-Map-Redesign zugunsten der Redesign-Version aufgelöst** — deren `_build_players_map()` kopiert im Light-Modus die komplette vorherige players-Map (inkl. `ml_prediction`) unverändert weiter, mein Fix war dadurch strukturell überflüssig geworden.
- [x] **Players-Map-Redesign vollständig umgesetzt und gemerged** (anderer Agent, eigener Worktree `worktree-smooth-inventing-popcorn`, alle 21 Plan-Tasks): jeweils Implementer-Subagent + dedizierte Task-Review, mehrere mit Fix-Runden.
  - Backend (Tasks 1-9): `points_avg`→`average_points`-Umbenennung, `_build_players_map()`/`_build_transfermarkt_listings()`/`_build_wunschkader_targets()` neu, 14 tote Ableitungsfunktionen gelöscht, `export()` neu verdrahtet, `_resolve_is_light()` erkennt Alt-Schema-Snapshots als Cold-Start (Selbstheilung), `migrate_wunschkader_player_ids.py` (einmaliges Migrationsskript — **danach ausgeführt und wieder gelöscht, siehe unten**).
  - Frontend Shared Lib (Tasks 10-14): `types.ts` neue Wire-Typen, `derive.ts` (Bewertung, DST-sicherer Auktions-Status, Builder-Funktionen, Budget-Plan), `wunschkaderResolve.ts` (geteilter `player_id`-Resolver).
  - Frontend Tab-Migrationen (Tasks 15-19): Alle 5 Tabs (Alle Spieler, Transfermarkt, Spekulation, Eigenes Team, Wunschkader) von altem Schema auf `players`-Map umgestellt, gemeinsamer Live-Ticker (`useNow`) in `App.tsx` konsolidiert.
  - Cleanup (Tasks 20-21): Alte optionale Typen aus `types.ts` entfernt, alte `index.html` + `/old/`-GitHub-Pages-Deploy retiriert.
  - **3 Korrekturen an bereits abgenickten Tasks** (im Branch selbst gefunden, vor dem Merge): (1) DST-Bug in `nextUpdateCutoff()` — bis zu 1h falsch in einem ~9h-Fenster/Jahr um die beiden Zeitumstellungen, gefixt per Zwei-Pass-Offset-Auflösung am Ziel-Zeitpunkt statt an `now`, verifiziert per erschöpfendem minütlichem Sweep (30.965+ Stichproben, 0 Abweichungen); (2) Budget-Regression — 4 Tasks hatten versehentlich die schon gefixte `actual_bid`+10%-Schätzungs-Logik wiedereingeführt statt echter Transfermarkt-Gebotsdaten (beide Hälften von `43c4854`/`fe40a39` betroffen, beide korrigiert); (3) `RawWunschkaderTarget` verlor sein `position`-Feld im neuen Schema — gelöst durch Live-Auflösung pro Spieler statt Speicherung auf dem Ziel-Objekt.
  - **Finale Whole-Branch-Review** (vor dem Merge) fand + fixte 5 weitere Punkte: Absturz-Guard in `App.tsx` gegen Alt-Schema-Snapshots (kein Error-Boundary vorhanden, White-Screen-Risiko — **genau dieser Fall trat nach dem Merge live ein, siehe nächster Punkt**), tote `PlayerRow`/`EigenesTeamRow`-Typ-Duplikate entfernt, `SpekulationTab` auf geteilten `auction_status` umgestellt, `SpekulationRow` um `player_id` ergänzt (React-Key-Kollisionsrisiko), `kForPosition` gegen fehlendes `position_k` abgesichert.
  - Merge lief in ZWEI Schritten (der Branch bekam nach dem ersten Merge noch 2 weitere Commits, die separat nachintegriert wurden). Merge-Konflikte gab es nur in `HANDOFF.md` (beide Seiten schrieben parallel) und `src/dashboard_export.py` (`_resolve_heavy_data`s Light-Zweig, siehe ML-Prognose-Bug-Eintrag oben) — beide von Hand aufgelöst, alle anderen ~20 Dateien mergten automatisch konfliktfrei.
- [x] **Bug: Weißer Bildschirm nach dem Merge-Deploy** (live gefunden 2026-07-30, sofort gefixt): Frontend wurde gepusht/deployed, aber der Firestore-Snapshot war noch im ALTEN Schema (letzter Backend-Lauf lag vor dem Push) — `data.players` war `undefined`, `.filter()` darauf crashte alles (der Absturz-Guard aus der finalen Review griff hier NICHT, weil er erst mit demselben Merge kam, der Frontend UND diesen Guard gleichzeitig auslieferte, aber das Backend noch nicht nachgezogen hatte). Fix: `dashboard-marktwerte.yml` (Heavy) manuell angestoßen → neues Schema live, dann `migrate_wunschkader_player_ids.py` gegen Produktion ausgeführt (15/15 Ziele aufgelöst) → Skript danach gelöscht (Task 9 abgeschlossen).
- [x] **sellSignal nur noch nach ML-Prognose** (Commit `5b18516`, User-Fund 2026-07-30): Verkaufskandidaten-Badge ("Jetzt verkaufen"/"Noch halten") hing zusätzlich an einer nie über die UI editierbaren Firestore-Liste (`wunschkader_sell_list`) — Bensebaini (+152k Prognose), Batista Meier, Ceka zeigten trotzdem "Jetzt verkaufen". Jetzt einheitlich: positive ML-Prognose → "Noch halten", sonst "Jetzt verkaufen". Feld komplett entfernt (Backend + `types.ts`).
- [x] **Spec + Plan: Gebotsvorschläge + Positions-Bedarfs-Analyse** (Commit `7071953`): `docs/superpowers/plans/2026-07-30-gebotsvorschlaege.md`, 13 Tasks per `superpowers:writing-plans`, basiert auf der bereits fertigen Spec vom 2026-07-29. **Nicht in dieser Session implementiert** — bewusst an einen separaten Agenten/Worktree delegiert (User-Präferenz, siehe Failed Approaches/Key Decisions), Kickoff-Prompt wurde übergeben.
- [x] **Brainstorming + Spec + Plan: Ligaanalyse-Detailansicht** (Commits `46623ca`/`1ea74b4`): `docs/superpowers/specs/2026-07-30-ligaanalyse-detailansicht-design.md` + `docs/superpowers/plans/2026-07-30-ligaanalyse-detailansicht.md`, 5 Tasks. Klick auf JEDE Manager-Karte (auch die eigene) öffnet ein Detail-Modal: kurzer Header (Platz/Punkte/Budget) + nach Position gruppierte Kaderliste (Name/Marktwert/Stammspieler-Status, keine weiteren Attribute). Datenquelle: neues `squad_player_ids`-Feld pro `_build_ligaanalyse()`-Row (aus bereits abgerufenem `get_manager_squad()`/`own_squad`, keine neuen API-Calls), Rest client-seitig über `data.players` aufgelöst. **Baut auf dem POST-Gebotsvorschläge-Stand von `_build_ligaanalyse()` auf** (Vorbedingungs-Check in Task 1) — explizite User-Entscheidung, dieser Plan geht davon aus, dass Punkt 1 zuerst gemergt wird. Ebenfalls delegiert, nicht hier implementiert.
- [x] **Cron-Kadenz angepasst** (Commit `1d29fe7`, User-Beobachtung: neue Transfermarkt-Angebote erscheinen oft kurz vor der vollen Stunde): Light (`dashboard.yml`) von "alle 2h zu :15" auf `0 * * * *` (stündlich, volle Stunde) geändert. Heavy (`dashboard-marktwerte.yml`) von 21:10 UTC auf `5 21 * * *` (21:05 UTC / 22:05 Berlin-Winterzeit) verschoben.
- [x] **Spekulation: Hype-Gipfel/Boden-Schutz-Pills komplett entfernt** (Commit `a44d420`, User-Wunsch — gefielen nicht, nicht nur in der Kartenansicht nachziehen): `is_hype_gipfel`/`near_floor` inkl. der zugehörigen `isHypeGipfel()`/`isNearFloor()`-Ableitungslogik aus `derive.ts`/`SpekulationTab.tsx` restlos entfernt, kein Firestore-Feld betroffen (war rein client-seitig abgeleitet).
- [x] **README/Workflow-Kommentare aktualisiert** (Commit `f9f9a1e`): keine Erwähnung der entfernten `index.html` mehr, README nennt jetzt korrekt 7 Tabs (statt 3) + beide Cron-Jobs (Light/Heavy getrennt beschrieben) mit aktueller Kadenz, `frontend-pilot.yml`-Kommentar beschreibt nicht mehr den längst entfernten `/old/`-Fallback.
- [x] **Detaillierter Dead-Code/Dead-Field-Audit durchgeführt und umgesetzt** (Commit `3990488`): weitere tote Firestore-Felder (`team_total_value`, `wunschkader_updated_at`, `calibrated_at`, `market_value_in_drop_phase`/`leading_bid_username` im Frontend-Pfad) + tote Funktionen (`upsert_wunschkader()`, `costPerPoint()`/`trendDirection()`) entfernt, stale `index.html`/Cron-Kommentare korrigiert. `MlMetrics`-Rohfelder bewusst behalten (werden gebraucht). War in einer früheren HANDOFF-Fassung noch als "läuft" markiert — tatsächlich schon abgeschlossen.
- [x] **Ungenutzte Firestore-Felder entfernt** (Commit `3402a7b`): `total_points` (aus `_build_players_map()`) und `pending_offers_count` (aus `_build_transfermarkt_listings()`) wurden im Firestore-Snapshot geschrieben, aber nirgends im Frontend gelesen — aus `dashboard_export.py`, `types.ts`, `derive.ts` entfernt. **Wichtig**: beide Felder werden weiterhin von der separaten `fetcher.py`/`db.py`/`prompt_builder.py`-Pipeline (Discord/Claude-WebUI-Prompt-Feature, eigenes SQLite, nichts mit dem Dashboard/Firestore zu tun) genutzt — dort NICHT angefasst.
- [x] **App-weiter Security-Audit** (Repo ist jetzt public): Firestore-Rules/CI/Backend/Frontend/Git-History geprüft, alles saubar bis auf 3 kleine Fixes: `permissions: contents: read` in beiden Backend-Workflows, `requirements.txt` auf exakte Versionen gepinnt, Wunschkader-Notiz-Textarea `maxLength={500}`. Zwei Punkte blieben damals User-Handschritte (Firebase-Console: Rules-Deployment-Abgleich, Auth-Email/Passwort-Setting) — der Rules-Abgleich ist seit `firestore-rules-deploy.yml` (siehe unten) automatisiert, nur das Auth-Email/Passwort-Setting bleibt offen.
- [x] **ML-Tab-Klarheits-Fixes + Bug**: X-Achsen-Beschriftung, Label-Kollision bei nahen Graphen, "Kopf an Kopf"-Text korrigiert (90 statt 30 Tage), MAE als Vortages-Richtungsgenauigkeit erklärt. **Root-Cause-Bug** `_infer_today()` in `src/market_predictor.py`: `today_iso` griff auf zukünftige Fixture-Zeilen (mv=NaN) zu statt auf den letzten Tag mit echtem Marktwert — `realized_by_model` blieb dadurch dauerhaft `None` trotz 60 Tagen echter `ml_accuracy_daily`-Daten. Live verifiziert: Modellauswahl wechselt jetzt korrekt zu `realized_trailing_30d`.
- [x] **Live-Modell-MAE ergänzt**: `liveModelMae()` (derive.ts) zeigt "(± MAE)" neben der ML-Prognose in Spekulation-Detail und EigenesTeam-Kader-Detail.
- [x] **App-weite Feld-Reihenfolge + Naming-Überarbeitung** (alle Ansichten, nach Entscheidungsrelevanz statt der alten willkürlichen Reihenfolge sortiert): EigenesTeam (Karte+Detail, inkl. neuem `StatusLabelRow`), Spekulation, Transfermarkt, Wunschkader, AlleSpieler. "Status" → **"Fitness"** (Verletzt/Angeschlagen/Fit) vs. **"Verfügbarkeit"** (Markt-Status) disambiguiert. "Budget"/"Cash" → **"Kapital"** (exakter Kontostand) vs. **"Budget"** (inkl. Kredit/Überziehung) vereinheitlicht, neue Kapital/Budget-Summenzeile über der Transfermarkt-Tabelle.
- [x] **Auktions-Zwei-Stufen-Dringlichkeit**: `auction_critical` (≤60min bis Ende, unabhängig von `auction_urgent`) in Spekulation+Transfermarkt als ⏰-Badge ergänzt. "Leistbar"-Spalte entfernt (durch die neue Kapital/Budget-Summenzeile ersetzt), Gebotsempfehlungs-Disclaimer-Absatz aus beiden Detail-Modals entfernt (User: "brauche ich nicht").
- [x] **Bug: Mitspieler-Transferangebote zeigten fälschlich "abgelaufen"** (`src/fetcher.py`, live widerlegt — ein 5 Tage altes Angebot war noch aktiv): die `mpst`-Tage-Schätzung für `_compute_expiry()` bei fehlendem echtem `exs`-Feld war schlicht falsch. Schätzung ersatzlos entfernt, Frontend zeigt jetzt ehrlich "kein Zeitlimit ermittelbar (gelistet seit ...)" statt einer falschen Behauptung.
- [x] **Bug: `bid_premium.py`-Pointer verlor Käufe dauerhaft** (live gefunden beim Backfill-Wunsch des Users, zweistufig gefixt, siehe Failed Approaches): Zeiger rückte vor BEVOR der Marktwert aufgelöst war — jeder nachfolgende Fehler verlor den Kauf permanent. Fix unterscheidet PERMANENT (Spieler nicht mehr in players_map, Zeiger rückt trotzdem vor) von TRANSIENT (Marktwert-Historie-Fehler/kein Wert am Kauftag, Zeiger friert ein). Live verifiziert: 86 von 107 historischen Käufen aufgelöst, 17 permanent fehlende akzeptiert (kein Positions-/Punkteschnitt-Quelle für sie verfügbar), Zeiger korrekt bei den 4 tagesaktuellen Käufen eingefroren (lösen sich mit dem nächsten Heavy-Lauf selbst).
- [x] **Gebotsvorschläge + Positions-Bedarfs-Analyse UMGESETZT** (13 Tasks, `docs/superpowers/plans/2026-07-30-gebotsvorschlaege.md`, separater Agent/Worktree): `suggestBid()` (Ähnlichkeits-gewichtete Perzentile aus `bid_premium_history`), `position_need` (Deckungsgrad pro Position), Transfermarkt-Spalte+Detail-Modal, Spekulation-Detail-Erweiterung. Gemerged, gepusht (Teil der 6 Commits, mit denen main schon vor diesem HANDOFF-Update begonnen hatte).
- [x] **Gebotstracking UMGESETZT, gemerged UND live verifiziert** (9 Tasks, `docs/superpowers/plans/2026-07-30-gebotstracking.md`, separater Agent/Worktree, Commit `15b504f`): `bought_by_self`-Tag pro `bid_premium_log`-Eintrag, `detect_unsold_listings()` (unverkauft abgelaufene Systemangebote per Listing-ID-Diff gegen den Activity-Feed, KEINE neuen API-Calls), neue Collections `bid_premium_unsold_log`/Pointer `last_seen_system_listing_ids`. Anzeige als Zähler-Aggregat im umbenannten Tab **"Modell-Tracking"** (vorher "ML-Genauigkeit", `key` unverändert für localStorage-Kompatibilität). Ein Merge-Konflikt in `tests/test_bid_premium.py` (zwei parallel eingefügte Testblöcke, additiv aufgelöst). **Explizit reine Beobachtung** — fließt NICHT in `suggestBid()` ein. **Bug beim Live-Test gefunden+gefixt**: alle 86 Alt-Einträge (vor diesem Feature geschrieben) hatten gar kein `bought_by_self`-Feld, `_build_outcome_counts()` behandelte das als `False` — alle historischen Käufe zeigten faelschlich "0 eigene". Einmaliges Backfill-Skript (`src/backfill_bought_by_self.py`, per `workflow_dispatch`, danach wieder gelöscht) hat 84/85 Alt-Einträge nachträglich korrekt getaggt. User hat den Tab danach live mit echten Zahlen bestätigt.
- [x] **Ligaanalyse-Detailansicht UMGESETZT und gemerged** (5 Tasks, `docs/superpowers/plans/2026-07-30-ligaanalyse-detailansicht.md`, separater Agent/Worktree, Commit `009f752`): jede Manager-Karte (auch die eigene) ist jetzt klickbar, Detail-Modal zeigt nach Position gruppierte Kaderliste (Name/Marktwert/Stammspieler-Status). `squad_player_ids` neu pro `_build_ligaanalyse()`-Row, `groupSquadByPosition()` in `derive.ts`. Konfliktfrei gemergt. **Browser-Test vom User bestätigt (2026-07-31): funktioniert.**
- [x] **Spieler-Vergleichsansicht UMGESETZT, gemerged UND gepusht** (Spec `docs/superpowers/specs/2026-07-30-spieler-vergleich-design.md` + Plan `docs/superpowers/plans/2026-07-30-spieler-vergleich.md`, 6 Tasks + 2 Review-Runden, separater Agent/Worktree, Commit `b536495`): geteiltes `PlayerCompareModal` (2 Spieler nebeneinander, "wer ist besser"-Hervorhebung inkl. Marktwert-niedriger-besser), 3 Einstiegspunkte (Wunschkader-Ersatzspieler-Suche mit Tausch-Auslöser, EigenesTeam+AlleSpieler rein informativ mit "Vergleichen mit…"-Button), neue `PlayerNamePicker`-Komponente, "Wechseln"-Swap innerhalb des offenen Vergleichs. **Fund während der Plan-Recherche**: AlleSpielerTab hatte bisher GAR KEIN Detail-Modal — Plan baute eins neu. Gegen den echten Code verifiziert, 2 Bugs im ersten Entwurf gefunden+gefixt (siehe Failed Approaches). Konfliktfrei gemergt, tsc + 161 Tests grün. **Danach live 3 Bugs gefunden** (User-Test nach dem Merge): Positions-Zwang im Wunschkader-Add-Dialog + unlesbarer Dark-Mode-Select-Text (beide durch denselben Fix behoben, Commit `e6eaef2`) und Freitext-Suchergebnisse sollten direkt tauschen statt zu vergleichen (ebenfalls `e6eaef2`, plus Self-Replace-Guard + Escape-Capture-Fix in `a719204` — alle drei von dieser Session, nicht von einer fremden).
- [x] **Contract-Test für `export()`s Snapshot-Key-Set** (Commit `faaf7cc`, aus der finalen Branch-Review empfohlen): `_assemble_snapshot()` aus `export()` extrahiert (trennt Key-Set von Login/Firestore/Kickbase-API), `AssembleSnapshotContractTests` prüft das exakte erwartete Key-Set ganz ohne Mocking. Fängt künftig genau die Bug-Klasse, die den Weißen-Bildschirm-Vorfall verursacht hat.
- [x] **Drei ML-Verbesserungs-Experimente durchgeführt** (2026-07-30, Walk-Forward-Backtest, alle einmalig per `workflow_dispatch` + danach wieder entfernt): HGB-Hyperparameter-Tuning, `mp_avg_3`-Verletzungs-Proxy, relativer statt absoluter Zielwert. **Keins hat die Baseline geschlagen** — siehe Failed Approaches für Details. `mp_avg_3`-Berechnung bleibt als totes, aber getestetes Code-Stück in `_performance_frame()` (nicht in `FEATURES` aktiv).
- [x] **Wunschkader-UX-Fixes** (2026-07-31, Commits `6e254ea`/`244d1b7`/`d6566ae`/`b1f7b81`/`b1025fd`): (1) echter Kaufpreis für besessene Ziele — Kickbase liefert im `get_squad()`-Response ein Feld `mvgl` (Market-Value-Gain/Loss seit Kauf), `mv - mvgl` = exakter Kaufpreis, **live verifiziert gegen 6 unabhängig aus `bid_premium_history` rekonstruierte Preise, exakte Übereinstimmung** — neue Spalte `purchase_price` durch `fetcher.py`→`db.py`→`_build_players_map()`→`PlayerRecord` gezogen, deckt ALLE Kaderspieler ab (nicht nur die per Systemangebot gekauften). "Geplanter Preis" in der Wunschkader-Detailansicht wird bei besessenen Zielen jetzt durch "Tatsächlicher Kaufpreis" ERSETZT (nicht ergänzt). (2) Fehlende Textfarbe im Add-Dialog-Suchergebnis behoben (Dark-Mode unlesbar) + Auswahl-Zustand jetzt mit explizitem "✓ Ausgewählt: ..."-Hinweis sichtbar. (3) Speichern-Meldung korrigiert (behauptete fälschlich "~2h", Light-Cron läuft stündlich; Text erklärt jetzt auch, dass die aktuelle Ansicht selbst sofort aktualisiert ist). (4) Cron-Minuten verschoben (`:17`/`:07` statt `:00`/`:05`) als Versuch gegen die GitHub-Actions-Lastspitze. **Ursprünglich vermuteter Bug** ("marktgelistete Spieler nicht auffindbar") stellte sich als Fehldiagnose heraus — der Owner-Filter ist korrekt, das echte Problem war das Hinzufügen selbst (Klick auf "Hinzufügen" tat nichts, kein Fehler). **Update 2026-07-31, live vom User bestätigt: funktioniert jetzt** — Textfarbe+Auswahl-Anzeige-Fix war tatsächlich die Ursache.
- [x] **Hotfix: `data/kickbase.db` aus Git entfernt** (Commit `6a423d6`, 2026-07-31): die committete SQLite hatte noch das alte Schema (kein `purchase_price`) - `CREATE TABLE IF NOT EXISTS` legt bei bereits existierender Tabelle keine neue Spalte an, jeder Light/Heavy-Lauf ist deshalb ab dem Kaufpreis-Push gecrasht ("table own_squad has no column named purchase_price"), bis dieser Hotfix landete. Datei war ohnehin nie ein echter Persistenz-Mechanismus (kein Workflow committet sie zurück) - jetzt gitignored, jeder Lauf erzeugt sie frisch. Live verifiziert: Kaufpreis danach korrekt in Firestore (z.B. Katic 11.650.176, exakt wie vorher unabhängig berechnet).
- [x] **`KICKBASE_LEAGUE_ID`-Secret jetzt wirksam** (Commit `4480c83`, 2026-07-31, User-Fund: gesetztes Secret hatte keine Wirkung): `select_league()` gab es nur als private Funktion in `fetcher.py`, genutzt NUR von `fetcher.run()` - `dashboard_export.py`/`market_predictor.py` (2x)/`player_valuation.py` pickten alle unabhängig ihr eigenes `leagues[0]`, das Secret komplett ignorierend (aktuell folgenlos, weil die gewünschte Liga "Kickb4se" zufällig bereits `leagues[0]` ist). Fix: `select_league()` nach `kickbase_client.py` verschoben (public), an allen 5 Stellen genutzt, Secret in beiden Workflow-`env`-Blöcken referenziert. Live verifiziert: "Account ist in 2 Ligen"-Warnung verschwindet aus dem Log.
- [x] **Wunschkader: Bank-Ziele fließen jetzt in die Budgetplanung ein** (Commit `934e697`, 2026-07-31, User-Korrektur): `buildBudgetPlan()` schloss Bank/Backup-Ziele bisher explizit aus der "Committed"-Summe aus - User: Rotationsspieler sind echtes gebundenes Budget, kein rein optionaler Gedanke. Zeile mit dem Ausschluss entfernt.
- [x] **Spekulation-Filter geklärt (kein Bug)**: ein Spieler mit negativer ML-Prognose (z.B. Assignon, -43k) taucht in Spekulation nicht auf, weil `roiPct()` (`derive.ts`) bei `mlPrediction <= 0` bewusst `null` zurückgibt und `buildSpekulationRows()` genau darauf filtert - Spekulation zeigt nur Systemangebote mit positiver erwarteter Rendite. Im normalen Transfermarkt-Tab ist er weiterhin sichtbar.
- [x] **Session-Korrektur: Sandbox hat echten Live-Zugriff auf Kickbase-API UND Firestore** (2026-07-31, live verifiziert): `.env` + `firebase-service-account.json` liegen im Repo-Root, funktionieren direkt aus der Sandbox. Alle ML-Experiment-Skripte dieser Session liefen unnötig über GitHub-Actions-`workflow_dispatch`-Umwege. Siehe Failed Approaches + [[project_kickbaseagent_sandbox_has_live_access]] falls Memory verfügbar.
- [x] **Live-Differential-Prüfung für `auction_status`** (2026-07-31, letzter offener Backlog-Quick-Win): `derive.ts`s `berlinParts`/`parseIsoZ`/`nextUpdateCutoff`/`auctionLabelAndRemaining`/`auctionStatus` sollten gegen ein Python-Äquivalent (`_parse_iso_z`/`_next_update_cutoff`/`_auction_status` in `dashboard_export.py`) gegengeprüft werden — diese Python-Funktionen existieren aber nicht mehr (per `grep` bestätigt, vermutlich beim Umzug der Ableitungslogik ins Frontend als toter Code entfernt). Stattdessen die TS-Logik 1:1 nach Python portiert (`zoneinfo`, DST-sicher ohne den in TS nötigen Zwei-Pass-Trick — Pythons `zoneinfo` löst das nativ korrekt auf) und gegen alle 52 echten, live aus Firestore gezogenen `transfermarkt_listings` laufen lassen. **0 Unstimmigkeiten**: bereits abgelaufenes Listing (Ljubičić, 5min überfällig) clamped korrekt auf `remaining=0`/beide Flags `false`; Kobel (19min) korrekt `urgent+critical`; Goto (4.93h, endet vor dem heutigen 22:00-Berlin-Cutoff) korrekt `urgent`, aber nicht `critical`. Cutoff-Berechnung selbst ebenfalls unabhängig nachgerechnet (14:22 CEST jetzt → 22:00 CEST heute → 20:00 UTC), stimmt exakt. Kein Fix nötig, Backlog-Punkt geschlossen.
- [x] **Fitness-Historie: Sammel-Pipeline + Read-Pfad + ML-Integration LIVE** (2026-07-31, 5-Task-Plan `docs/superpowers/plans/2026-07-31-fitness-historie.md` + finale Review-Fixrunde). Ersetzt den früheren "Echte Verletzungshistorie aufbauen"-TODO: (1) **`fitness_history_log`-Collection** — diff-basiert, EIN Dokument pro tatsächlichem `status_code`-Wechsel (nicht ein Tages-Snapshot pro Spieler), Doc-Id `{date}_{player_id}` macht Wiederholungsläufe am selben Tag idempotent. Geschrieben **nur im Heavy-Cron** (1x/Tag, dort wo `all_players` frisch gefetcht wird); Firestore-Fehler werden abgefangen und dürfen den kritischen `dashboard_snapshot`-Write nicht verhindern. (2) **`fitness_status_baseline/latest`** — eigenes, dediziertes Diff-Baseline-Dokument (flaches `player_id -> status_code`-Dict), das **nur** die Heavy-Diff-Logik anfasst und das der stündliche Light-Cron nie berührt. **Wurde erst im finalen Review nachgezogen, so NICHT wie ursprünglich geplant ausgeliefert**: der Plan las die Baseline aus `dashboard_snapshot/latest`, dessen `players`-Map der Light-Cron aber stündlich überschreibt und dabei `status_code` für `own_squad`/`market_listings`-Spieler frisch überlagert — ein Wechsel, den ein Light-Lauf zwischenzeitlich eingebaut hat, wäre im nächsten Heavy-Diff schon "alt == neu" gewesen und damit **dauerhaft verloren** (kein Backfill möglich, Kickbase liefert keine Zeitreihe). Der Baseline-Write läuft in jedem Heavy-Lauf unbedingt (auch nach Lesefehler oder ohne Wechsel) → selbstheilende Startbasis. Nebeneffekt des Fixes: der im Plan neu eingeführte Heavy-Modus-`get_dashboard_snapshot()`-Read ist wieder entfernt (Heavy liest `dashboard_snapshot/latest` gar nicht mehr — wichtig wegen des Read-Quota-Vorfalls, siehe Warnings). (3) **Zwei neue ML-Features** in `market_predictor.py`s `FEATURES`: `days_since_last_status_change` und `status_change_count_90d` (aus `_fitness_features_as_of()`). **Beide zeigen aktuell für praktisch alle Spieler die Cold-Start-Platzhalter `9999`/`0`** — die Historie muss erst über Wochen/Monate anwachsen. Das ist **erwartet, kein Bug**; eine echte Aussage, ob die Features das Modell verbessern, ist frühestens nach einigen Wochen Datensammlung möglich (bis dahin sind sie effektiv konstant und damit für das Modell wertlos, aber auch nicht schädlich).
- [x] **Icons/Vereinswappen UMGESETZT** (2026-07-31, mehrere Commits): 18 selbst generierte Vereinswappen (`frontend/public/crests/*.svg`, Dateiname = 3-Buchstaben-Kürzel, Monogramm-Kreise mit eigener Vereinsfarbe als Hintergrund — **keine echten Logos**, bewusst um Markenrechtsfragen zu vermeiden, kontrastsicher gegen Light/Dark). `frontend/public/icons-src/` (Staging-Ordner, auf User-Wunsch **inkl. Generator-Tooling** committed: `badges.py`/`colors.py`/`preview.html` + finale SVGs — anders als bei `crests/`, wo das Tooling bewusst draußen bleibt). Neue `frontend/src/components/icons.tsx` (9 React-SVG-Icons, `fill="currentColor"`/`stroke="currentColor"` — erben Textfarbe, deshalb inline JSX statt `<img src>`). `ui.tsx` neu: `POSITION_ICON`/`STATUS_ICON`-Maps + `PositionBadge`/`FitnessBadge`-Komponenten (konsolidieren ~14 bzw. mehrfach duplizierte Inline-Badge-Stellen über 7 Tab-Dateien). `frontend/public/logo.svg` (feste Markenfarbe `#0fc46a`) in Login+Header verdrahtet. App-weite Tailwind-Palette (`brand`-Skala + `slate`, `darkMode: "media"`) — Kommentar im `tailwind.config.js` weist explizit darauf hin, dass die Markenfarben eine Annäherung sind, nicht verifiziert-exakt.
- [x] **Feedback-Tab "Bugs & Ideen" UMGESETZT** (2026-07-31, Spec `docs/superpowers/specs/2026-07-31-feedback-tab-design.md` + Plan `docs/superpowers/plans/2026-07-31-feedback-tab.md`, 2 Tasks + 1 Review-Fixrunde, Commits `fd9f2f2`/`21c0897`/`4591deb`/`9b6e136`/`d81401b`). **Frontend-only, kein Python-Backend-Code beteiligt**: neuer Tab schreibt DIREKT per Firebase-Client-SDK nach Firestore-Dokument `feedback/current` (Array-in-Dokument-Muster wie beim bestehenden `wunschkader/current`) — Einträge `{id, type: "bug"|"feature", text, created_at, status: "open"|"done"}`. Hinzufügen nutzt `arrayUnion` (kein vorheriges `getDoc`, kollisionsfrei gegen parallele Sessions), Bearbeiten macht echtes Read-Modify-Write gegen den FRISCH gelesenen Serverstand (nicht den evtl. veralteten lokalen State — verhindert, dass ein zwischenzeitlich von einer anderen Session gesetztes `status:"done"` wieder überschrieben wird). `firestore.rules` um `feedback`-Collection erweitert, gleiche Single-UID-Sperre wie `wunschkader`. Review-Fixrunde behob Erreichbarkeits-/Robustheits-/Datenverlust-/Offline-Feedback-Findings (u.a. der "Speichert…"-Hänger-Hinweis nach 6s ohne Serverantwort, siehe Code-Kommentar zu Firestores Offline-Queue-Verhalten). **Wichtig für jede neue Session**: dies ist ein ZWEITER Eingangskanal für Arbeit an diesem Projekt, unabhängig vom Chat — siehe Warnings unten, aktiv `feedback/current` auf offene (`status:"open"`) Einträge prüfen, nicht nur auf Chat-Anfragen reagieren.
- [x] **Mobile-UX: Swipe-Gesten + Tap-Ziele** (2026-07-31, 4 Commits `c227f57`/`9ab727b`/`2724d06`/`2311893`): horizontales Wischen wechselt den aktiven Tab (Inhalt folgt dem Finger, wie iOS-Fotogalerien — Richtung nach User-Test einmal umgedreht). Zwei Guards verhindern Fehlauslösung: `modalOpenTracker` blockt Swipe komplett bei offenem Detail-Modal, `data-swipe-ignore` auf Tabellen-Wrappern blockt bei horizontalem Tabellen-Scroll. Zusätzlich: Mindest-Tap-Höhe 44px + Schließen-Buttons vergrößert, kein Auto-Focus mehr auf Mobile-Inputs (verhindert ungewolltes Aufklappen der virtuellen Tastatur).
- [x] **Aktiver Tab wird per `localStorage` persistiert** (Commit `0ddb152`): App startet nicht mehr immer auf Spekulation, sondern auf dem zuletzt offenen Tab (`kickbaseagent_active_tab`).
- [x] **`generated_at`-Feld im Snapshot + "Stand: ..."-Anzeige im Header** (Commit `4933228`): exakter Zeitstempel statt nur Datum, `formatRelativeTime()` im Frontend. Bereits über `AssembleSnapshotContractTests.EXPECTED_KEYS` testabgesichert.
- [x] **TransfermarktTab-Detailansicht auf Spekulation-Niveau angehoben** (Commit `662edd6`): ML-Prognose+MAE, 7-Tage-Trend, Tief/Hoch, Delta%, Auktions-Status, Anbieter — vorher deutlich schlankere Detailansicht als Spekulation.
- [x] **AlleSpielerTab: Marktwert-Filter erlaubt jetzt leeren Zwischenzustand beim Tippen** (Commit `9ac9f17`, kleiner UX-Fix).
- [x] **Neuer CI-Workflow `firestore-rules-deploy.yml`** (Commit `280ec03`): deployt `firestore.rules` automatisch bei Änderungen — macht den früher hier dokumentierten manuellen Schritt "Firebase-Console: Rules-Deployment-Abgleich" überflüssig. **Von den beiden ursprünglich als Security-Audit-Nachtrag dokumentierten Handschritten bleibt nur noch einer offen**: Firebase-Auth-Email/Passwort-Setting (siehe Setup Required).
- [x] **`backfill_prediction_log()` auf 3-Tage-Horizont generalisiert + live gegen Produktion ausgeführt** (2026-07-31, Commit `deae6b6`, per Subagent-Implementierung + dedizierter Code-Review, verifiziert durch aktives Aufbrechen+Zurücksetzen beider Kern-Fixes): `target_col`/`horizon_days`-Parameter mirroren exakt das Muster von `_train_and_evaluate`/`_walk_forward_backtest`, inkl. desselben NaN-Zeilen-Schutzes (Test-Fold wird vor der Fehlerberechnung auf die ungeklippte Zielspalte hin von NaN befreit — ohne den Schutz hätte ein 3-Tage-Backfill exakt den schon einmal gefundenen Bug reproduziert, bei dem eine einzelne NaN-Zeile das MAE-Aggregat für BEIDE Modelle über den GESAMTEN Lauf auf NaN kippt). CLI (`__main__`) ruft die Funktion jetzt zweimal auf (Horizont 1 und 3). Schreibt in dieselbe `ml_accuracy_daily`-Collection wie der tägliche Heavy-Cron, per `horizon_days`-Doc-Id-Dimension kollisionsfrei getrennt. **90-Tage-Live-Lauf (beide Horizonte) gegen Produktion abgeschlossen (2026-07-31)**: 1-Tage-Horizont 90 Folds/180 Tages-Aggregate, 3-Tage-Horizont 88 Folds/176 Tages-Aggregate (2 weniger, weil deren tatsächlicher Ausgang am aktuellen Rand der Historie noch nicht bekannt ist — erwartet, kein Fehler). Insgesamt 356 Firestore-Writes für den ganzen Lauf, **weit unter dem 20.000-Writes/Tag-Limit** des Spark-Free-Tiers (kein Wiederholungsrisiko des Vorfalls vom 28.07., der auf dem längst ersetzten Rohdaten-Schreibpfad beruhte). Live per Read gegen Produktion verifiziert: `ml_accuracy_daily` hat jetzt 482 Dokumente total, davon 176 mit `horizon_days:3` (exakt wie geloggt), inkl. sinnvoller Beispielwerte (z.B. `2026-05-02`/HistGradientBoosting: n=331, sign_correct=191, d.h. ~57.7% Richtungsgenauigkeit für diesen einen Tag über den 3-Tage-Horizont — einzelne Tageswerte sind erwartungsgemäß verrauscht, erst das Aggregat über viele Tage ist aussagekräftig).

- [x] **ML-Horizonte: 3-Tage-Prognose + Konfidenz-Signal LIVE** (2026-07-31, Spec `docs/superpowers/specs/2026-07-31-ml-horizonte-design.md` + 9-Task-Plan `docs/superpowers/plans/2026-07-31-ml-horizonte.md`, 3 Task-Fixrunden + 1 finale Review-Fixwelle inkl. Re-Review — CLEAN). Zweiter Prognose-Horizont neben dem bisherigen 1-Tages-Signal, primär für Verkaufszeitpunkt-Planung UND Spekulations-Kauftiming: (1) **`TARGET_3D`/`mv_target_3d_clipped`** (`shift(-3)`, gleiches IQR×2.5-Clipping wie der 1-Tage-Target) — bewusst NICHT im gemeinsamen `dropna`-Filter, sonst würde der 1-Tages-Trainingskorpus durch die NaN-Tail-Zeilen des 3-Tage-Ziels schrumpfen (eigener, jetzt auch testabgesicherter Regressionsschutz). `_train_and_evaluate`/`_walk_forward_backtest`/`backfill_prediction_log` sind alle per `target_col`-Parameter generalisiert (Default = 1-Tage, unverändertes Verhalten). (2) **`_train_and_track_horizon()`** kapselt Training+Tracking pro Horizont; der 3-Tage-Aufruf hat eine EIGENE `try/except`-Klammer — eine Exception dort darf das bereits berechnete 1-Tages-Ergebnis nicht mit zerstören (im finalen Review als Critical-Lücke im ursprünglichen Plan-Text selbst gefunden, nicht nur ein Implementierungsfehler). (3) **`horizon_days`** als neue Dimension im Firestore-Doc-Id-Schema (`ml_prediction_log`/`ml_accuracy_daily`, `.get("horizon_days", 1)` für Alt-Einträge — keine Migration nötig). `EVALUATION_LOOKBACK_DAYS` wurde im finalen Review erweitert (`+ horizon_days - 1`), weil die alte feste 3-Tage-Fensterbreite dem 3-Tage-Horizont praktisch keinen Cron-Ausfall-Puffer ließ (ein verpasster Heavy-Lauf hätte den Tag dauerhaft verloren). (4) **Frontend**: `momentumAssessment()` (`derive.ts`) — Konfidenz-Einstufung ("sicher"/"wahrscheinlich"/"unsicher") rein aus 1-Tages-Prognose vs. MAE, die 3-Tages-Prognose relativiert nur den Nachsatz (bestätigt/Rauschen-Hinweis). `sellSignal()` von 2-wertig auf 3-wertig ("halten"/"unklar"/"verkaufen") umgebaut — **"unklar" bei Prognose ≤ MAE, identische Grenze wie `momentumAssessment()`s "unsicher"**, jetzt cross-referenziert per Kommentar. **Badge-Farben geändert** (User-approved): "verkaufen" jetzt rot/crit (vorher amber), "unklar" amber/warn. `momentumAssessment()` in allen 4 Detail-Modals verdrahtet (Eigenes Team, Wunschkader, Spekulation, Transfermarkt) — Wunschkader/Spekulation/Transfermarkt brauchten dafür jeweils neue Props (`mae`/`player`/`players`), die es vorher nicht gab. **`ml_metrics_3d`/`ml_accuracy_trend_3d` werden geschrieben, haben aber bewusst noch KEINE eigene UI-Anzeige** — nur das kombinierte `momentumAssessment()`-Signal nutzt sie indirekt. Cold-Start: `ml_metrics_3d.realized_by_model` ist bis genug 3-Tages-Historie vorliegt `None`, wie beim 1-Tages-Pendant damals.

- [x] **Wunschkader-Live-Sync-Bug gefixt: Eigenes Team zeigt jetzt sofort den aktuellen Wunschkader-Stand nach dem Speichern** (2026-08-01, Spec `docs/superpowers/specs/2026-08-01-eigenes-team-wunschkader-live-sync-design.md` + 4-Task-Plan `docs/superpowers/plans/2026-08-01-eigenes-team-wunschkader-live-sync.md`, User-Bug aus `feedback/current`). Ursache: `wunschkader/current` wurde nur einmal beim App-Start als Teil einer eingefrorenen Snapshot-Kopie gelesen — ein Save in `WunschkaderTab` änderte Firestore, aber nie den State, den `EigenesTeamTab` tatsächlich rendert. Fix: `App.tsx` liest `wunschkader/current` jetzt über einen eigenen, unabhängigen `getDoc` (mit Fallback auf `snapshotData.wunschkader_targets ?? []` bei fehlendem Dokument/Fehler, kein Realtime-Listener), hält den Stand in eigenem State, reicht ihn an `EigenesTeamTab`/`WunschkaderTab` durch. `WunschkaderTab.handleSave()` ruft nach erfolgreichem `setDoc` einen neuen `onSaved(targets)`-Callback, der den App-State direkt aktualisiert — kein Reload nötig, Formation bewusst unverändert (bleibt weiterhin aus `data.wunschkader_formation`, war explizit nicht Teil dieses Fixes). Commits `ab695f0`/`2adacef`/`8c3d54e`. Backend-Tests unberührt (226/226 weiterhin grün), `tsc --noEmit` 0 Fehler. Feedback-Item (`feedback/current`, erstellt 2026-08-01T10:12:37.394Z) auf `status: "done"` gesetzt.
- [x] **ML-Horizonte im Frontend sichtbar gemacht: Prognose 1T/3T statt "ML-Prognose"/"Einschätzung"** (2026-08-01, Spec `docs/superpowers/specs/2026-08-01-ml-horizonte-frontend-anzeige-design.md` + 7-Task-Plan `docs/superpowers/plans/2026-08-01-ml-horizonte-frontend-anzeige.md`, User-Feedback aus `feedback/current`). `momentumAssessment()` (qualitative Konfidenz-Text-Zeile) komplett gelöscht, ersetzt durch zwei rohe Zahlen überall dort, wo vorher "ML-Prognose" stand: EigenesTeam (Karten UND beide Detail-Modals), Transfermarkt (neue Tabellenspalte "Prognose 3T"), Spekulation (Karte UND Tabelle UND Modal), Wunschkader (Detail-Modal bekam die Anzeige neu — hatte vorher gar keine rohe ML-Prognose gezeigt). `PlayerRow`/`SpekulationRow` (derive.ts) um `ml_prediction_3d` erweitert. Modell-Tracking-Tab zeigt jetzt zwei Kopf-an-Kopf-Blöcke + zwei Trend-Charts (1-Tages-/3-Tages-Horizont), Kopf-an-Kopf-Block+Trend-Chart-Überschrift dafür in parametrisierte Komponenten extrahiert. **3T-Trend-Pfeil-Einfärbung bewusst noch nicht umgesetzt** (reine Zahl mit Vorzeichen) — eigene Schwellenwerte kommen als Nebenprodukt der 3-Tage-Hyperparameter-Suche (`docs/superpowers/specs/2026-08-01-ml-3d-tuning-design.md`), siehe Not Yet Done. Commits `eb135b3`/`18247ab`/`6b362c1`/`27b7309`/`cbf6f31`/`f697a33` + finale Review-Fixrunde `ec18b05` (tote `fmtNum`/`fmtSigned`-Imports in `derive.ts` entfernt, `SpekulationTab.tsx`s Sort-Label/Hinweistext/Empty-State auf "Prognose 1T" statt "ML-Prognose" vereinheitlicht — war in Task 4 übersehen worden, da Task 3s analoge Umbenennung nicht 1:1 mitgezogen wurde, erst im finalen Whole-Branch-Review über den Gesamtdiff sichtbar). Backend-Tests unberührt (226/226 weiterhin grün), `tsc --noEmit` 0 Fehler. Feedback-Item (`feedback/current`, erstellt 2026-08-01T07:18:41.006Z) auf `status: "done"` gesetzt.
- [x] **Tabellen/Karten-Toggle für Transfermarkt und Alle Spieler** (2026-08-01, 4-Task-Plan `docs/superpowers/plans/2026-08-01-card-view-toggle.md`): Transfermarkt- und Alle-Spieler-Tab bekommen denselben Tabellen/Karten-Toggle, den Spekulation schon hatte. Neuer geteilter Hook `useViewMode(storageKey)` (`frontend/src/lib/useViewMode.ts`) kapselt Default-Erkennung (`window.matchMedia("(max-width: 639px)")` — Handy startet mit Karten, Desktop mit Tabelle, EIN Check beim Mount, kein Resize-Listener) + `localStorage`-Persistenz je Tab (`kickbaseagent_view_spekulation`/`_transfermarkt`/`_alle_spieler`, kein geteilter Zustand). `SpekulationTab.tsx` migriert auf den Hook — **Verhalten ändert sich dadurch auf Desktop tatsächlich**: vorher fix `"cards"` unabhängig von der Bildschirmbreite, jetzt wie die beiden anderen Tabs Default "Tabelle" auf Desktop (>639px) beim ersten Laden (kein gespeicherter Wert), Karten nur auf Handy — im finalen Review bewusst als Spec-Intention bestätigt, nicht als Regression. `TransfermarktTab.tsx`/`AlleSpielerTab.tsx` bekommen je eine neue Card-Komponente (kuratierte Feldauswahl, Vorbild `SpekulationCard`) + denselben Toggle, Kartenklick ruft denselben `onSelect`/`setSelected`-Callback wie der Tabellen-Zeilen-Klick. **`TransfermarktCard` musste beim Umsetzen gegen den inzwischen von `ml-horizonte-frontend-anzeige` veränderten Live-Code reconciled werden** (der Plan wurde vor jenem Plan geschrieben, hatte das aber selbst schon als Abhängigkeits-Warnung vermerkt): kein separater `player`/`PlayerRecord`-Prop mehr nötig, `row.ml_prediction_3d` direkt, Labels "Prognose 1T"/"Prognose 3T" statt "ML-Prognose". Commits `24507cb`/`84d9086`/`81461d9`. Backend-Tests unberührt (226/226 weiterhin grün), `tsc --noEmit` 0 Fehler. **Nachtrag 2026-08-01**: der Plan zitierte kein `feedback/current`-Item, obwohl er exakt einem entspricht ("Transfermarkt und Alle Spieler auch Kartenansicht, hier auch togglebar...", erstellt 2026-08-01T07:22:14.210Z) — beim Cross-Check der offenen Items nachträglich gefunden und auf `status: "done"` gesetzt.
- [x] **Prognose 3T in `PlayerCompareModal.tsx` + Naming-Konsistenz `AlleSpielerTab.tsx`** (2026-08-01, direkter User-Wunsch im Chat, kein eigener Plan): schliesst die von `ml-horizonte-frontend-anzeige` bewusst ausgesparte Lücke — beide Dateien zeigten weiterhin nur "ML-Prognose" (1T) ohne 3T. `PlayerCompareModal.tsx` bekommt eine neue "Prognose 3T"-Zeile (`fmtSigned`, kein Trend-Pfeil, wie überall sonst bei 3T), `AlleSpielerTab.tsx`s Detail-Zeile ebenso ergänzt, beide Labels auf "Prognose 1T"/"Prognose 3T" umbenannt. Kein neuer Datenmodell-Bedarf — `ml_prediction_3d` war über `PlayerRow` bereits verfügbar. Commit `92109cc`. Backend-Tests 226/226 grün, `tsc --noEmit` 0 Fehler.
- [x] **3T-Trend-Pfeil-Einfärbung nachgezogen — die von `ml-horizonte-frontend-anzeige` bewusst ausgesparte Lücke** (2026-08-02, `worktree-ml-3d-tuning`, direkt gegen aktuellen `main`-Stand statt gemergt — main hatte inzwischen unabhängig dieselbe Anzeige-Basis + zusätzlich `PlayerCompareModal`/`AlleSpielerTab` gebaut, ein Merge des Worktrees hätte auf denselben 6 Dateien in Konflikt gestanden). **Schwellenwerte NICHT aus der 3-Tage-Hyperparameter-Suche** (die fand am Ende keinen echten Gewinner, siehe Failed Approaches/`docs/superpowers/plans/2026-08-01-ml-3d-tuning-results.md` — beeinflusst diese Einfärbung nicht, unabhängige Fragen), sondern per Perzentil-Rang-Matching gegen die bestehenden 1-Tages-Schwellen aus `data/ml_prediction_log.jsonl` (17890 Zeilen, 816 mit `horizon_days:3`) — identische Methodik wie die bestehenden 1-Tages-Werte: `ML_PREDICTION_3D_THRESHOLDS = { flat: 210_000, strong: 420_000 }` (Rang 0.349/0.688, Ziel aus den 1-Tages-Werten 0.356/0.687). Angewendet in allen 6 Dateien, die "Prognose 3T" zeigen: `EigenesTeamTab`, `TransfermarktTab` (Spalte+Modal), `SpekulationTab` (Karte+Tabelle+Modal), `WunschkaderTab`, `AlleSpielerTab`, `PlayerCompareModal` — überall derselbe `trendClass`/`trendArrow`-Wrap wie bei Prognose 1T, nur mit der neuen Konstante. Backend unberührt (226/226 grün, reine Frontend-Änderung), `tsc --noEmit` 0 Fehler, `npm run build` clean.
- [x] **README.md-Overhaul: nur noch Architektur + lokales Setup** (2026-08-01, User-Wunsch): Hosting/Deploy-Details (GitHub Pages, Workflow-Cadences, wo/wie live erreichbar) bewusst raus — laut User zwar im Code erkennbar, aber nicht im README vorne sichtbar gewünscht. Neue Struktur: Architektur-Übersicht aller `src/`-Module + Datenhaltung (SQLite lokal, Firestore als Snapshot-Store) + Frontend, dann rein lokale Setup-/Ausführungs-Anleitung. Commit `96486f1`.
- [x] **Header/Browser-Tab entschlackt + Favicon** (2026-08-01, User-Wunsch): Header zeigt nur noch "KickbaseAgent" (Zusatz "Dashboard" raus), Browser-Tab-Titel gekürzt von "KickbaseAgent Dashboard (Preview)" auf "KickbaseAgent", neues `<link rel="icon">` auf `logo.svg` — per echtem `npm run build` verifiziert, dass Vite den Pfad korrekt auf den `/KickbaseAgent/`-Base-Path umschreibt (`href="/KickbaseAgent/logo.svg"` im gebauten `dist/index.html`). Commit `d559692`.
- [x] **Mobile Burger-Menü statt Tab-Leiste** (2026-08-01, User-Wunsch, Swipe zwischen Tabs sollte explizit erhalten bleiben): neue `IconMenu` (`icons.tsx`), neue `MobileTabMenu`-Komponente in `App.tsx` — volle Liste (inkl. "(bald)"-Sperren für noch nicht aktive Tabs, identische Logik wie die Desktop-`<nav>`) als seitliches Overlay, nur `sm:hidden`. Die horizontale `<nav>`-Tab-Leiste bleibt ab `sm:` (Desktop) unverändert sichtbar (`hidden sm:flex`), die bestehende `useSwipeTabs()`-Wischlogik ist komplett unabhängig von der Nav-Darstellung (liest nur aus den `TABS`/`ACTIVE_TABS`-Konstanten) — dadurch unangetastet lauffähig. `MobileTabMenu` registriert sich per `useModalOpenTracking()` wie jedes andere Modal, damit ein Wischen bei offenem Menü nicht versehentlich den Hintergrund-Tab wechselt (`isAnyModalOpen()`-Guard in `useSwipeTabs()`, bereits bestehend). Commit `75d6504`. Backend-Tests 226/226 grün, `tsc --noEmit` 0 Fehler, `npm run build` erfolgreich.
- [x] **Wunschkader: alle 10 erlaubten Formationen + live abgeleitete Positions-Buttons statt starrer Combobox** (2026-08-01, Spec `docs/superpowers/specs/2026-08-01-wunschkader-formationen-design.md` + 4-Task-Plan `docs/superpowers/plans/2026-08-01-wunschkader-formationen.md`, User-Fund aus `feedback/current`): vorher nur 4 von tatsächlich 10 in Kickbase erlaubten Formationen konfiguriert (Recherche per WebSearch/WebFetch, Drittquelle DAZN — Kickbases eigene Hilfe-Seite listet keine Details, siehe Spec). Formation wird nicht mehr per Combobox vorausgewählt, sondern live aus der tatsächlichen Ziel-Belegung abgeleitet (`canAddStarter()`/`matchedFormation()`, `formations.ts`) — Add-Button pro Position nur sichtbar, solange mindestens eine der 10 Formationen noch erreichbar ist, read-only Anzeige zeigt die exakt erreichte Formation sobald 11 Feldspieler stehen. `wunschkader_formation` komplett aus dem Stack entfernt (Backend `dashboard_export.py`, Contract-Test, `types.ts`) — keine Migration nötig, da die neuen 10 Formationen jede bisher gespeicherte Ziel-Belegung weiterhin zulassen. Commits `d4c7237`/`53b206d`/`7d50041`/`e1e522c`. Backend-Tests: volle Suite bestätigt grün (`Ran 226 tests ... OK`, 954s statt der üblichen ~30s — Verzögerung kam von einem zeitgleich laufenden, separaten legitimen Parallel-Job dieser Session (`ml-3d-tuning`-Hyperparameter-Suche, ~1360% CPU), kein Zusammenhang mit diesem Feature). `tsc --noEmit` 0 Fehler. Feedback-Item (`feedback/current`, erstellt 2026-08-01T10:34:27.589Z) auf `status: "done"` gesetzt. **Finales Review-Fix**: Formations-Anzeige zeigte bei einer per "In Startelf verschieben" erzwungenen ungültigen Belegung (z.B. 2 Torwärter, keine Formations-Machbarkeitsprüfung bei diesem Toggle) irreführend "noch nicht komplett" statt auf die Ungültigkeit hinzuweisen — Anzeige unterscheidet jetzt explizit "ungültige Aufstellung" von "noch nicht komplett".
- [x] **Icons an den Wunschkader-Aktions-Buttons** (2026-08-01, Spec `docs/superpowers/specs/2026-08-01-wunschkader-action-icons-design.md` + 2-Task-Plan `docs/superpowers/plans/2026-08-01-wunschkader-action-icons.md`, User-Feedback aus `feedback/current`): die 3 Aktions-Buttons im `WunschkaderTab.tsx`-Detail-Modal (Bank/Startelf-Toggle, Wechsel, Entfernen) hatten bisher reinen Text. 4 neue Icons in `icons.tsx` nach dem bestehenden Muster (`IconActionBank`/`IconActionField`/`IconActionSwap`/`IconActionTrash`, `fill="currentColor"`, erben Textfarbe in Light/Dark-Mode). Buttons zeigen jetzt Icon + auf ein Wort gekürzten Text ("Auf Bank verschieben"→"Bank", "In Startelf verschieben"→"Startelf", Wechsel/Entfernen waren schon je ein Wort) — Toggle-Button wechselt Icon je nach `isBench(target)`. Reine visuelle Ergänzung, `onClick`-Handler/Logik unverändert. Commits `2265f08`/`0a122fe`. `tsc --noEmit` 0 Fehler. `IconActionSwap`s Bogen-Pfade wurden ohne visuelle Vorschau konstruiert — beim manuellen Browser-Test besonders prüfen, ggf. Pfad-Werte nachjustieren. Feedback-Item (`feedback/current`, erstellt 2026-08-01T07:23:02.312Z) auf `status: "done"` gesetzt.

## Not Yet Done

- [ ] **`_walk_forward_backtest()` hat kein Embargo für Mehrtage-Horizonte — verzerrt vermutlich die bereits LIVE gezeigte 3-Tage-Genauigkeit** (2026-08-02, Folge-Fund aus der 3-Tage-Hyperparameter-Suche, siehe Failed Approaches + `docs/superpowers/plans/2026-08-01-ml-3d-tuning-results.md`): eine Trainingszeile mit Datum `cutoff-1` trägt beim 3-Tage-Ziel ein Label, das Marktwerte NACH dem Test-Cutoff kodiert (`mv[cutoff+2]`) — ohne Embargo (Ausschluss der letzten `horizon_days` Trainingstage vor jedem Cutoff) kann das je nach Modellkomplexität ausgenutzt werden. Betrifft nur `horizon_days=3` (beim 1-Tages-Ziel existiert das Leck nicht). Die bereits im Frontend sichtbare `ml_metrics_3d`/Modell-Tracking-3T-Genauigkeit ist davon betroffen — vermutlich etwas zu optimistisch. **Nicht nebenbei fixen** — würde bereits live gemeldete Genauigkeits-Zahlen rückwirkend verändern, verdient eine eigene, bewusste Session (Embargo einbauen, dann echte Zahlen neu ansehen, ggf. User informieren dass die bisher gezeigte 3T-Genauigkeit etwas geschönt war).
- [ ] **Plan: Modelle nochmal (deep) tunen, sobald genug echte Daten da sind** (2026-07-31, User-Idee, noch nicht begonnen): die letzte große Hyperparameter-Suche (277 Konfigurationen, 9h, siehe Failed Approaches) lief NUR gegen den 1-Tages-Horizont, ohne Fitness-Features (die gab's damals noch nicht) und ohne den 3-Tages-Horizont. Kandidat für einen erneuten Lauf, sobald (a) die Fitness-Features (`days_since_last_status_change`/`status_change_count_90d`) genug echte Historie haben, um nicht mehr konstant zu sein, UND (b) der 3-Tages-Horizont genug echte `ml_accuracy_daily`-Einträge hat, um eine sinnvolle Baseline zu haben (der 90-Tage-Backfill dafür ist jetzt gelaufen, siehe Completed/Current State — beschleunigt das erheblich gegenüber dem Warten auf den täglichen Cron). **Nicht von selbst anfangen** — User hat das als Idee genannt, kein aktueller Auftrag; anzusprechen, sobald genug Zeit vergangen ist, dass die Fitness-Features nicht mehr nur Cold-Start-Platzhalter zeigen (siehe den Fitness-Historie-Punkt weiter oben).
- [ ] **Playwright Component Testing für `WunschkaderTab` umsetzen** (`docs/superpowers/plans/2026-07-30-playwright-component-testing.md`, gemerged, noch NICHT implementiert): 2 Regressionstests, ohne Firebase/Auth (Vite-Alias-Mock). User hat sich noch nicht zwischen Subagent-Driven/Inline entschieden. Der "Hinzufügen"-Bug (Task 3 dieses Plans) ist jetzt live bestätigt gefixt (siehe Completed) - der Test würde also jetzt tatsächlich PASS zeigen, nicht mehr nur der ungeprüften Plan-Annahme nach. Reiner Regressionsschutz für die Zukunft, kein akuter Bedarf.
- [ ] **Cron-Minuten-Verschiebung beobachten** (2026-07-31, `:17`/`:07` statt `:00`/`:05`): war die Nacht 30./31.07. ein einmaliger mehrstündiger Komplettausfall oder ein wiederkehrendes Muster? `gh run list --workflow=dashboard.yml --limit 30 --json createdAt,event` nach ein paar Tagen erneut prüfen.
- [ ] **Fitness-Historie: auf Daten warten, dann die zwei ML-Features ehrlich bewerten** (ersetzt den alten "Echte Verletzungshistorie aufbauen"-Punkt — die Pipeline selbst ist gebaut und live, siehe Completed): `fitness_history_log` sammelt ab jetzt echte Statuswechsel, aber `days_since_last_status_change`/`status_change_count_90d` liefern bis auf Weiteres nur Cold-Start-Platzhalter. Erst nach Wochen/Monaten Datenaufbau per Walk-Forward-Backtest prüfen, ob sie die Baseline schlagen — vorher hat eine Messung keinen Aussagewert. Bis dahin **nicht** an den Features herumoptimieren. Weiterhin offen bleibt eine Historie *vor* dem 31.07.2026 — die existiert nirgends und ist nicht rekonstruierbar.
- [ ] Bekannte kleine Rest-Punkte aus dem Backlog (unverändert, nicht angefragt, nicht bearbeiten ohne Nachfrage): externe Signale wie Transfermarkt.de-Wechselgerüchte (explizit als "noch komplexer, später" zurückgestellt). Autopilot-Idee (schreibende Kickbase-API-Calls) ist eine reine Neugier-Frage, kein Auftrag, explizit nicht für die aktuelle Liga gedacht. **ML-Prognose-Horizonte (3-Tage+) ist NICHT mehr offen — siehe Completed, live seit 2026-07-31.**
- [ ] **Autopilot-Idee (sehr früh, reine Neugier-Frage, 2026-07-30, KEIN aktueller Auftrag)**: könnte man über die Kickbase-API auch schreibende Aktionen auslösen (z.B. automatisch Gebote abgeben)? Technisch plausibel — gleiches Auth/Token wie beim Lesen, die Kickbase-App nutzt für Gebote/Käufe ja auch API-Calls, aber dieser Endpunkt wurde in diesem Projekt noch nie exploriert (bisher nur Read-Endpunkte reverse-engineered). Erster Schritt bei Interesse: Network-Capture aus der echten App während eines Gebots, um Endpunkt/Payload zu finden. **Explizit NICHT für die aktuelle Liga gedacht** (User will hier weiterhin selbst klicken) — höchstens gedanklich für eine separate Test-Liga interessant. Größerer Punkt dabei: Sprung von reinem Decision-Support (Vorschlag, Mensch klickt) zu automatisiertem Handeln im eigenen Namen — anderes Risikoprofil (ToS-Bann-Risiko bei vielen Fantasy-Plattformen für automatisierte Aktionen, ein Bug würde echtes In-Game-Budget verbrennen statt nur falsche Zahlen anzuzeigen). Nicht nebenbei einbauen, sondern falls überhaupt als eigenes, bewusst gescopetes Feature mit Sicherheitsnetz (z.B. Vorschlag + manuelle Bestätigung statt Vollauto-Bid).

## Failed Approaches (Don't Repeat These)

- **3-Tage-Hyperparameter-Suche fand scheinbaren Gewinner, der sich als Daten-Leck-Artefakt entpuppte** (2026-08-01/02, `docs/superpowers/plans/2026-08-01-ml-3d-tuning.md` + `docs/superpowers/plans/2026-08-01-ml-3d-tuning-results.md` fuer die volle Analyse, im inzwischen geloeschten Worktree `worktree-ml-3d-tuning` durchgefuehrt). 325-Konfigurationen-Suche (11h) fand scheinbar `RandomForest(n_estimators=800, min_samples_split=2, min_samples_leaf=1, max_features="sqrt", max_depth=None)` bei 86.6%/MAE=91060 gegen `TARGET_3D` (Baseline 82.4-82.6%/91231-93224) - klar ueber dem Sign-Accuracy+1pt/MAE<=Baseline-Kriterium, zunaechst probeweise in `_build_candidates()` uebernommen. **Finale Whole-Branch-Review (opus) fand einen plausiblen Daten-Leck-Mechanismus**: beim 3-Tage-Ziel kodiert eine Trainingszeile mit Datum `cutoff-1` ein Label, das Marktwerte NACH dem Test-Cutoff enthaelt (`mv[cutoff+2]`) - ein unbegrenzt tiefer Random Forest kann das ueber spielerspezifische Feature-Autokorrelation ausnutzen (existiert beim 1-Tages-Ziel NICHT). Indizien: bimodale Ergebnis-Verteilung mit leerer Luecke zwischen 82.8%/85.2%, Effekt tritt nur bei `max_depth=None` auf (nicht bei `max_depth=30`), Gewinn fast rein in Accuracy, nicht in MAE. **Embargo-Nachmessung bestaetigte den Verdacht**: Baseline verliert unter Embargo (Trainingszeilen der letzten `horizon_days` vor Cutoff ausgeschlossen) nur -0.3pt, der Gewinner verliert -2.8pt (~9x mehr) - unter fairer (Embargo-)Messung erfuellt der Gewinner das MAE-Kriterium nicht mehr. **Konsequenz**: keine Hyperparameter-Aenderung uebernommen, `_build_candidates()`/`_train_and_evaluate()`/`_walk_forward_backtest()` unveraendert. **Lektion**: bei Mehrtage-Zielen mit ueberlappenden Label-Fenstern IMMER ein Embargo in die Walk-Forward-Eval einbauen, bevor eine Hyperparameter-Suche darauf vertraut wird - sonst werden unregularisierte/tiefe Modelle systematisch bevorzugt, unabhaengig vom Kriterium. Siehe auch den entdeckten Folge-Bug (fehlendes Embargo in der PRODUKTIVEN `_walk_forward_backtest()`, Not Yet Done oben).
- **Falsche Annahme: Sandbox hätte keinen Netzwerk-/Credential-Zugriff auf Kickbase/Firestore.** Diese Annahme wurde die ganze ML-Tuning-Phase dieser Session (2026-07-30) nie verifiziert, sondern einfach behauptet - inkl. gegenüber dem User ("meine Sandbox hat keine Kickbase-Zugangsdaten"). Live-Check 2026-07-31 (`load_dotenv()` + `login()` direkt in der Sandbox) zeigte: FALSCH, funktioniert direkt. `.env` und `firebase-service-account.json` liegen im Repo-Root (gitignored, aber ueber den Windows-Mount vorhanden), beide funktionsfaehig. **Lektion: bei einer Sandbox-Faehigkeits-Annahme ("ich kann X nicht") diese explizit testen, bevor ein aufwaendigerer Umweg (hier: mehrere GitHub-Actions-Workflows mit je mehreren Minuten Setup-Overhead) gebaut wird.** Siehe [[project_kickbaseagent_sandbox_has_live_access]] falls Memory verfuegbar.
- **Fehldiagnose: Owner-Filter im Wunschkader-Add-Dialog als Bug gemeldet, war aber korrekt.** Erste Diagnose eines User-Bug-Reports ("Spieler beim Hinzufuegen nicht gefunden") identifizierte den Owner-Filter (nur "Frei"/"Eigener Kader") als Root Cause und plante einen Fix. User-Korrektur: der Filter ist gewolltes Verhalten, das eigentliche Problem war das tatsaechliche HINZUFUEGEN nach erfolgreicher Suche, ein anderer Schritt im selben Flow. **Lektion: bei mehrstufigen UI-Flows ohne Live-Repro-Moeglichkeit die genaue Fehlerstelle als Hypothese kennzeichnen, nicht vorschnell committen.** Siehe [[feedback_confirm_exact_failure_point]] falls Memory verfuegbar.
- **ML-Modell-Verbesserung: erste 3 schnellen Experimente erfolglos, danach echte Verbesserung durch groessere Suche gefunden** (2026-07-30/31). Erste Runde (Walk-Forward-Backtest, nur 6 Folds - im Rueckblick zu klein, siehe naechster Punkt): (1) HGB-Hyperparameter-Tuning (6 Konfigs) - keine schlaegt die (verzerrte) Baseline. (2) `mp_avg_3` (rollierender 3-Spiele-Minutenschnitt als Verletzungs-Proxy, da echte historische Verletzungsdaten nicht existieren) - macht MAE leicht schlechter. Code bleibt drin (`_performance_frame()`), NICHT in `FEATURES` aktiv. (3) Relativer statt absoluter Zielwert - verschlechtert MAE massiv (kleine Marktwerte verstaerken Rauschen prozentual). **User-Fund**: Live-Kachel zeigte 82.8%/25931 (n=10659, echte 30 Tage), mein 6-Fold-Backtest hatte optimistische 90.1%/22553 (n=2244) behauptet - zu kleine Stichprobe. Mit 30 Folds (n≈10962, fast identisch zur echten Stichprobe) bestaetigt: RF/HGB/LightGBM/XGBoost alle bei ~82.0-82.4%, ExtraTrees/Ridge klar raus. **Nacht-Runde** (User liess es lokal 9h laufen, `TUNING_BUDGET_HOURS=9`, 277 randomisierte Konfigurationen ueber RF/HGB/LightGBM/XGBoost, 30 Folds): echte Verbesserung gefunden - mehrere Konfigurationen aus 3 Bibliotheken konvergierten auf 83.2-83.4%/MAE~25000-25160 (viele Iterationen 200-500, niedrige Lernrate 0.02-0.05, hohe Blatt-Kapazitaet 63-127). **Uebernommen**: `_build_candidates()`s HistGradientBoosting auf `{learning_rate:0.05, max_iter:200, max_leaf_nodes:127, min_samples_leaf:20, l2_regularization:0.0}` (83.4%/25147 statt 82.4%/25370) - kein neues Dependency noetig (LightGBM war minimal davor, aber HGB bleibt reines sklearn). **Nebenbei gefixt**: `_walk_forward_backtest()` hatte eine eigene, laengst veraltete Kopie der Modell-Parameter (RF n_estimators=200 statt 500) - nutzt jetzt `_build_candidates()` statt zu duplizieren. Echte Verletzungsdaten und externe Signale (Wechselgeruechte etc., noch komplexer) bleiben die einzigen noch offenen, aufwaendigeren Wege fuer eine WEITERE Verbesserung. **Update 2026-07-31**: die Verletzungs-/Fitness-Historie wird jetzt tatsaechlich gesammelt (`fitness_history_log`, siehe Completed) und ist als zwei Features in `FEATURES` verdrahtet - aber noch ohne Daten (Cold-Start-Platzhalter), also weder als Erfolg noch als Misserfolg gemessen. Der frueher hier verlinkte Injury-History-TODO-Merkzettel ist inzwischen aus dem Claude-Memory entfernt worden (2026-07-31, war nur noch der Ausgangszustand, nicht der Live-Stand) — dieser HANDOFF-Abschnitt UND der Fitness-Historie-Completed-Eintrag oben sind jetzt die einzige Quelle dafuer.
- **Erster Fix-Versuch für `bid_premium.py`s Pointer-Bug war unvollständig**: Zeiger einfach beim ERSTEN Fehler jeder Art einfrieren zu lassen (statt nur bei Marktwert-Auflösungs-Fehlern) hätte den Zeiger dauerhaft blockiert, weil 17 von 22 fehlenden Käufen strukturell PERMANENT sind (Spieler nicht mehr in players_map, z.B. abgestiegen) — jeder Lauf hätte alle 107 historischen Käufe erneut komplett durchprobiert. Erst live verifiziert (Eintrags-Anzahl blieb nach dem ersten Fix unverändert bei 86, Zeiger blieb `None`) wurde der Fehler sichtbar. **Lektion: bei zwei Fehlerklassen mit unterschiedlicher Permanenz nicht dieselbe Zeiger-Reaktion für beide verwenden — live verifizieren, nicht nur am Test-Verhalten ablesen.**
- **Plan-Recherche fand 2 echte Bugs, bevor sie geschrieben wurden** (Spieler-Vergleich-Plan, 2026-07-30): `DetailModalShell`s `children` landen direkt in einem `<dl>` (ui.tsx) — ein rohes `<button>` dort reinzuhängen wäre ungültiges HTML, erst durch direktes Lesen der Datei (nicht aus der Session-Erinnerung) gefunden, Fix: neuer `footer`-Prop. Zweiter Fund: ein zusätzliches `fixed inset-0`-Overlay-Modal braucht in `WunschkaderTab.tsx`s `DetailModal` einen Fragment-Wrapper um den bestehenden `return`, sonst wäre es falsch genestet. **Lektion, konsistent mit [[feedback_verify_plan_against_live_code]]: bei jedem Implementierungsplan die betroffenen Dateien VOR dem Schreiben der Tasks tatsächlich lesen, nicht nur aus dem Konversationskontext rekonstruieren.**
- **Plan-Code kann bereits abgenickte Tasks später als falsch entlarven — cross-task-Konsistenz nicht blind vertrauen.** Der `actual_bid`-Bug (siehe Completed, Korrektur 2) steckte in VIER bereits einzeln reviewten Tasks (4, 10, 11, 13), weil jede Task-Review nur gegen ihre eigene, vom Plan gegebene Referenz (u.a. bereits veraltete Python-Quelle) geprüft hatte, nicht gegen das tatsächlich aktuell schon gefixte Live-Verhalten in `WunschkaderTab.tsx`. **Lektion: bei einem Redesign, das bestehende, bereits gefixte Logik "nur portieren" soll, immer die aktuell laufende Implementierung als Ground Truth nehmen, nicht die vom Plan mitgelieferte Quelle — die kann selbst schon veraltet sein.** Wurde erst beim Cross-Check vor Task 19 (letzter, komplexester Tab) entdeckt, weil dort zum ersten Mal alle Puzzleteile zusammenkamen.
- **Ein Plan-Code-Block kann seinem eigenen Kommentar widersprechen ("1:1 Port") — im Zweifel dem erklärten Ziel folgen, nicht dem Wortlaut.** `derive.ts`s `valuation()` (Task 11) rundete `fairwert` nicht, obwohl der Kommentar "1:1 Port von `_valuation()`" versprach und das Python-Original rundete. Gleiches Muster beim DST-Cutoff (Kommentar "DST-sicher", Code war es in einem Randfall nicht). **Bei solchen Widersprüchen: die Diskrepanz dem Implementer/Reviewer explizit benennen und unabhängig verifizieren lassen, nicht den Code kommentarlos für bare Münze nehmen.**
- **API-Überlastung (529 Overloaded) während der Session** (kurzer, ca. 1h Ausfall laut status.claude.com, betraf alle Modelle) — mehrere Subagent-Dispatches schlugen fehl. **Reaktion, die funktioniert hat**: sofortiger Retry zuerst versucht, danach alle 15 Minuten per `ScheduleWakeup` erneut versucht (User-Vorgabe), bis ein Retry durchging — kein Datenverlust, da fehlgeschlagene Agent-Dispatches nie committet hatten (SDD-Ledger macht genau diese Wiederaufnahme robust, siehe unten).
- **App-weite Feld-Reihenfolge-Anpassung (`07fe96a`) hat versehentlich auch Ligaanalyse mitgezogen, obwohl das nicht gemeint war** (Commits `e60623d`/`5c35507`, 2026-07-31): ein "das passt so" des Users bezog sich nicht auf ALLE gerade angepassten Tabs, sondern nur auf einen Teil — Ligaanalyse hätte unverändert bleiben sollen. Zurückgebaut (`e60623d`), danach Kapital/Budget-Naming gezielt wiederhergestellt, aber die ursprüngliche Feldreihenfolge diesmal bewusst NICHT angefasst (`5c35507`). **Lektion: bei einer Sammelbestätigung ("passt so") über mehrere gleichzeitig geänderte Dateien/Tabs hinweg im Zweifel nachfragen, welche Teile genau gemeint sind, statt die Bestätigung pauschal auf den gesamten Diff anzuwenden.**
- (Aus früheren Sessions, weiterhin gültig) **Subagent-Ergebnisse nie ungeprüft übernehmen** — `git diff`/`git status` prüfen, bevor ein Agent-Report für bare Münze genommen wird.
- (Aus früheren Sessions, weiterhin gültig) **Status-Code-Bedeutungen nicht neu raten** — `MDs/codes.md` hat die verifizierte Zuordnung; nur bei neuem, unbeobachtetem Code (Code 8 offen) neu recherchieren, nur mit echtem In-App-Beleg.

## Key Decisions

| Decision | Rationale |
|----------|-----------|
| Players-Map: EIN Firestore-Dokument bleibt, nur intern als Map statt Arrays | Kein zusätzlicher Read, keine Quota-Verschlechterung; dünne Referenzlisten halten die Dokumentgröße klein |
| Atomarer Cutover, kein Firestore-Doppelschreiben | Hobby-Projekt, ein User; Selbstheilung über erweiterten `_resolve_is_light()`-Cold-Start-Check (altes Schema = Cold Start) |
| Alle ableitbaren Werte client-seitig, nur Rohdaten+ML-Prognose server-seitig | Explizite User-Entscheidung: Cron-Jobs sollen nur aktualisieren, "was wir nicht selbst errechnen können" |
| `actual_bid`-Feld + 10%-Schätzung ersatzlos entfernt (in dieser Session zweimal bestätigt — einmal vor, einmal während des Redesigns) | Manuelles Feld überflüssig, sobald echte Gebotsdaten (`is_own_leading_bid`/`leading_bid_price`) vorliegen |
| `position` nicht mehr auf Wunschkader-Zielen gespeichert, sondern live pro `player_id` aufgelöst | Vermeidet, dass ein gespeichertes Ziel eine andere Position zeigt als der tatsächliche Spieler — strukturell robuster als der alte Zustand |
| Finale Whole-Branch-Review vor Abschluss, mit einem gebündelten Fix-Wave statt Einzel-Fixes | SDD-Skill-Konvention: ein Fix-Dispatch für alle Findings, eine Re-Review, keine zweite Fix-Runde — hat hier 5 Findings sauber in einem Rutsch behoben |
| Feedback-Tab schreibt frontend-only direkt nach Firestore (kein Python-Backend-Code) | Gleiches Array-in-Dokument-Muster wie `wunschkader/current` bereits etabliert — kein neuer Cron-Pfad/API-Call nötig, Firestore-Security-Rules reichen als Schutz |
| ML-Horizonte: `target_col`/`horizon_days` als Parameter statt Code-Duplikation (`_train_and_evaluate`, `_walk_forward_backtest`, `backfill_prediction_log`) | Ein Bug-Fix (z.B. der NaN-Zeilen-Schutz) muss nur an einer Stelle passieren, nicht an N Kopien — genau dieses Muster hat den NaN-Bug beim 3-Tage-Backfill von vornherein verhindert |

## Current State

**Working**: Backend-Tests 226/226 grün (`python3 -m unittest discover -s tests`, Stand 2026-07-31 nach ML-Horizonte-Final-Review + `backfill_prediction_log()`-Generalisierung). Frontend `tsc` 0 Fehler. **8 Tabs** (nicht mehr 7, siehe Completed: Feedback-Tab dazugekommen) auf `players`-Map umgestellt, DST-sicherer Auktions-Countdown verifiziert, Budget-Logik konsistent. Cron-Kadenz: Light stündlich zu **`:17`** (`17 * * * *`, verschoben wegen beobachteter Lastspitzen-Ausfälle rund um volle Stunden), Heavy täglich **`19:04 UTC`** (`4 19 * * *` — **2026-08-01 nochmal verschoben** von `21:07 UTC`, siehe Session 2026-08-01 oben: kompensiert gemessenen GH-Actions-Delay, Ziel ~22:07 Berlin, plus neuer 12-21-Uhr-Guard-Step).

**2026-08-02: `main` ist wieder synchron mit `origin/main`** (verifiziert per `git rev-list --left-right --count origin/main...HEAD` → `0	0`, HEAD `3238602`). Der frühere Hinweis hier ("14 Commits voraus, Push-Entscheidung offen") ist überholt — alle damals lokalen Markdown-Commits und alles, was seitdem dazukam (Frontend-Coloring, Tuning-Ergebnis-Doku), ist längst gepusht. Keine Push-Entscheidung mehr nötig, bevor an einem der offenen Punkte weitergearbeitet wird.

**Broken**: Nichts Bekanntes.

**Uncommitted Changes**: `git status` zeigt weiterhin unstaged Änderungen an `MDs/codes.md`/`datencheck.md`/`liga-kontext.md`/`methodik.md`/`spieler-bewertung.md` + `frontend/package-lock.json` — **nicht von dieser Session, vermutlich User-eigener WIP-Stand**, bewusst nicht angefasst/gestasht, seit mehreren HANDOFF-Updates unverändert. `data/ml_prediction_log.jsonl` war zwischenzeitlich ebenfalls uncommitted (echter Nebeneffekt eines Live-Smoke-Tests von `predict_market_value_changes()` während der ML-Horizonte-Implementierung dieser Session, NICHT User-WIP wie hier zuvor fälschlich vermerkt) — per Commit `a3363a6` (2026-08-01) als Snapshot-Refresh eingecheckt, folgt damit dem etablierten Muster früherer Commits dieser Art für exakt diese Datei.

**Worktrees** (`git worktree list`, Stand 2026-08-02): 6 registriert — `card-view-toggle`, `card-view-toggle-plan`, `eigenes-team-wunschkader-live-sync` (locked), `wunschkader-action-icons`, `wunschkader-fixes`, plus `handoff-update` (locked, für dieses HANDOFF-Update selbst genutzt, danach wieder entfernt). **Die ersten 5 sind stale/orphaned** — die jeweiligen Features sind alle längst in `main` gemerged: Card-View-Toggle (`24507cb`/`84d9086`/`81461d9`), Live-Sync-Fix (`ab695f0`/`2adacef`/`8c3d54e`), Wunschkader-Action-Icons (`2265f08`/`0a122fe`), `wunschkader-fixes` war laut Korrektur weiter oben ohnehin dieselbe Session. `worktree-ml-3d-tuning` (früher hier gelistet) ist bereits gelöscht. Können bei Gelegenheit per `git worktree remove` aufgeräumt werden — vorher kurz prüfen, dass kein anderer Agent gerade darin arbeitet (`eigenes-team-wunschkader-live-sync` ist noch als `locked` markiert, obwohl der Code schon gemergt ist — evtl. vergessenes Unlock, nicht einfach überschreiben ohne kurz reinzuschauen).

## Files to Know

| File | Why It Matters |
|------|----------------|
| `docs/superpowers/plans/2026-07-29-players-map-datenstruktur.md` | Der vollständig umgesetzte Plan — 21 Tasks, alle committed. Bei Bedarf zum Nachvollziehen der ursprünglichen Absicht lesen, aber Vorsicht: mind. 2 Stellen darin sind durch die Korrekturen inzwischen veraltet (siehe Failed Approaches) |
| `.superpowers/sdd/2026-07-29-players-map-datenstruktur/progress.md` | SDD-Ledger dieser Session — jede Task-Review, jeder Fix-Round, alle geparkten/deferred Minor-Findings. Wird typischerweise nach Abschluss gelöscht (Git-History ist jetzt der Record), ist aber bis zum finalen Merge noch da |
| `src/dashboard_export.py` | Zentrale Backend-Export-Logik, komplett neu verdrahtet: `_build_players_map()`/`_build_transfermarkt_listings()`/`_build_wunschkader_targets()`, `_resolve_is_light()`/`_resolve_heavy_data()` |
| `src/migrate_wunschkader_player_ids.py` | Einmaliges Migrationsskript, **noch nicht gegen Produktion gelaufen** — siehe Not Yet Done, Reihenfolge ist wichtig (nach Push, vor oder nach dem ersten Heavy-Lauf, siehe Skript-Docstring) |
| `frontend/src/lib/derive.ts` | Neue geteilte Bibliothek — alle Bewertungs-/Auktions-/Budget-Formeln, inkl. des DST-Fixes und des `liveBidFor()`-Zusammenführungs-Fixes |
| `frontend/src/lib/wunschkaderResolve.ts` | Geteilter `player_id`-Resolver für Wunschkader + Eigenes-Team-Watchlist |
| `frontend/src/App.tsx` | Enthält jetzt den Alt-Schema-Crash-Guard (aus der finalen Review) — zeigt eine Hinweis-Meldung statt abzustürzen, wenn `data.players` fehlt |
| `frontend/src/types.ts` | Neues, bereinigtes Schema — alle Alt-Felder/-Typen entfernt (Task 20 + finale Review) |
| `src/market_predictor.py` | ML-Prognose-Kern. **`FEATURES` ist NICHT mehr der Stand von direkt nach den 3 erfolglosen Experimenten** (frühere Formulierung hier war veraltet): seit 2026-07-31 sind `days_since_last_status_change` + `status_change_count_90d` dazugekommen (aus `_fitness_features_as_of()`, gespeist aus `fitness_history_log`) — beide noch mit Cold-Start-Platzhaltern, siehe Completed/Not Yet Done. `TARGET`/`_build_candidates()`: `_build_candidates()`s HGB-Parameter stammen aus der 9h-Nacht-Tuning-Runde (83.4%), nicht aus der ersten Experiment-Runde. `_performance_frame()` berechnet `mp_avg_3` (Verletzungs-Proxy), aber es ist NICHT in `FEATURES` — bewusst dormant, siehe Failed Approaches |
| `src/firestore_db.py` | Alle Firestore-Zugriffe. Neu: `upsert/get_fitness_history_entries` (`fitness_history_log`) + `upsert/get_fitness_status_baseline` (`fitness_status_baseline/latest`, Diff-Baseline — bewusst NICHT `dashboard_snapshot/latest`, das der Light-Cron überschreibt; Docstring erklärt warum) |
| `src/dashboard_export.py::_assemble_snapshot()` | Neu extrahiert (Contract-Test) — jedes neue Top-Level-Snapshot-Feld MUSS hier UND im Test (`AssembleSnapshotContractTests.EXPECTED_KEYS`) ergänzt werden, sonst schlägt der Test fehl |
| `frontend/src/components/FeedbackTab.tsx` | 8. Tab, "Bugs & Ideen" — schreibt frontend-only direkt nach `feedback/current`, kein Python-Backend-Code beteiligt. Zweiter Eingangskanal für Arbeit an diesem Projekt, siehe Warnings |
| `firestore.rules` | Nur 3 client-schreibbare Pfade: `dashboard_snapshot` (read-only für die feste UID, Write nur per Admin-SDK), `wunschkader` (read+write), `feedback` (read+write, neu) — alles andere per Default-Deny-Fallback gesperrt. Wird seit `firestore-rules-deploy.yml` automatisch deployt bei Änderungen |
| `src/market_predictor.py::backfill_prediction_log()` | Einmalige Utility, jetzt `target_col`/`horizon_days`-parametrisiert (Default = 1-Tage, unverändertes Verhalten). Nicht Teil des täglichen Laufs — bei Bedarf manuell via `python3 -m src.market_predictor --backfill <TAGE>` (läuft inzwischen automatisch für BEIDE Horizonte hintereinander) |

## Resume Instructions

**Standing-Prozess (seit 2026-08-02, gilt für JEDE neue Session hier, ersetzt die alte rein chronologische Abarbeitung)**:

1. **Immer zuerst `feedback/current` in Firestore auslesen** (Collection `feedback`, Dokument `current`, Feld `items[]` — zweiter Eingangskanal neben dem Chat, siehe Warnings). Korrekter Python-Zugriff: `from google.cloud import firestore` (**nicht** `firebase_admin` — das Paket ist hier nicht installiert). `GOOGLE_APPLICATION_CREDENTIALS=firebase-service-account.json python3 -c "from google.cloud import firestore; ..."` reicht, kein venv nötig (`google-cloud-firestore` ist im System-`python3` bereits vorhanden, wie auch `src/firestore_db.py` es nutzt).
2. **Alle `status:"open"`-Items einsortieren** (User-Vorgabe, nicht mehr nur nach Priorität in einer Liste abarbeiten):
   - **Quick Win** — ohne viel Planung umsetzbar: kleiner, klar abgegrenzter Scope, keine offene Produktentscheidung.
   - **Need Planning** — braucht Planung, aber hoher User-Impact: zeitig angehen, nicht liegen lassen.
   - **Backlog** — braucht Planung, User-Impact aktuell nicht so hoch: läuft nicht weg, nicht proaktiv anstoßen.
3. **Erst danach** `git log --oneline -10` + `git worktree list` prüfen, BEVOR irgendein Plan/Feature ausgeführt wird — mehrere parallele Sessions/Agenten arbeiten regelmäßig gleichzeitig an diesem Repo (siehe Warnings), der Stand kann sich zwischen Chat-Nachrichten schon geändert haben.

**Kategorisierung Stand 2026-08-02** (14 offene Items, zur Orientierung — bei Sessionstart per Schritt 1 neu auslesen, es können neue Items dazugekommen oder inzwischen erledigt worden sein, nicht blind übernehmen):

**Quick Win**:
- Heavy-Cron "wieder nicht richtig getriggert" (`2464b2e6`) — der Timing-Fix (Cron auf `19:04 UTC` vorverlegt + Guard-Step, siehe Session-2026-08-01-Abschnitt oben) ist schon gepusht; vermutlich reicht `gh run list --workflow=dashboard-marktwerte.yml --limit 10 --json createdAt,conclusion`, um zu bestätigen, dass er greift, dann Item auf `status:"done"` setzen.
- Mobiler Header verschwindet beim Scrollen nicht kontextsensitiv (`832154f3`) — Standard-Pattern Hide-on-scroll-down/Show-on-scroll-up, rein im Mobile-Nav-Bereich.
- Wunschkader ohne Extra-Klick/Scroll speichern + Hinweistext vereinfachen (`f462d415`).
- "(Standard)"-Zusatz bei Sortier-Labels entfernen (`75b53c6e`) — trivialer Text-Fix.
- Kopf-an-Kopf-Kacheln 1T/3T im Modell-Tracking (Web-Breite) zu einer zusammenlegen (`4a19f09a`) — reines Layout.
- Tausendertrennzeichen in Transfermarkt-Zahlenfiltern (`d390f441`) — Format-on-blur/Parse-on-submit, gut abgegrenzt.
- Mobile Tab-Überschrift ergänzen (`2fbcfeca`) — seit dem Burger-Menü (Completed) fehlt die Orientierung, welcher Tab gerade aktiv ist.
- "Marktwert Update"-Hinweiszeile in Spekulation/Transfermarkt (`eefbbba8`) — rein kosmetisch, Kickbase-Vorbild.
- Zeichenlimit im Feedback-Formular erhöhen (`9e85fca8`) — `maxLength` in `FeedbackTab.tsx`.

**Need Planning** (hoher Impact, aber Produktentscheidung/Konzept nötig):
- **Tages-Dashboard mit Handlungsbedarf** (`bef54eff`) — der vom User mit Abstand ausführlichst beschriebene Wunsch (Verkaufs-/Kaufkandidaten + letzte Transfers anderer Manager gebündelt, Ziel: seltener direkt in Kickbase einloggen müssen). War schon einmal in Brainstorming, User hat selbst gestoppt ("das bedarf größerer Planung") — **nicht ohne Nachfrage neu anstoßen**, aber bei grünem Licht Priorität vor den anderen beiden Need-Planning-Items.
- **ML-Charts mobil kaum lesbar** (`5a182f9d`) — Tooltip-Position, Punktdichte, Zeitraum; braucht Entscheidungen zu Mobile-Breakpoints/Sampling, blockiert aber aktuell die mobile Nutzbarkeit des Modell-Tracking-Tabs.
- **Wunschkader "Geplanter Preis" live berechnen** (`297fc4aa`) — User nennt zwei Optionen (pauschal +10% auf Marktwert vs. den 75%-Schwellenwert aus der Gebotsempfehlung übernehmen) — Produktentscheidung, keine reine Implementierung.

**Backlog** (braucht Planung, aktuell nicht dringend):
- **Grafische Tab-Indikatoren** (`89aaaa7d`) — User selbst: "hierfür müssen Ideen gesammelt werden", noch keine konkrete Anforderung.
- **Sentiment-Analyse für Marktwert-Turning-Points** (`6b08e2cf`, Beispiele Amiri/Le Joncur) — groß angelegt (externe Nachrichtenquellen, Sentiment/NLP, neuer Hintergrund-Agent), spannend aber unscoped R&D. Eigene Brainstorming-Session wert, wenn der User bewusst Zeit dafür einplant — nicht proaktiv anfangen.

**Unabhängig von `feedback/current`, weiterhin gültig** (siehe Not Yet Done für vollen Kontext): Embargo-Fix für `_walk_forward_backtest()` bei Mehrtage-Horizonten (**nicht nebenbei fixen** — würde bereits live gezeigte 3T-Genauigkeit rückwirkend verändern, eigene Session), Playwright-Component-Testing-Plan für `WunschkaderTab` (gemerged, nicht umgesetzt, User noch unentschieden Subagent-Driven/Inline), "Modelle nochmal tunen sobald genug Daten da sind" (noch nicht — Fitness-Features liefern noch Cold-Start-Platzhalter).

**Worktree-Housekeeping** (siehe Current State): 5 der 6 registrierten Worktrees sind stale (Feature längst gemerged) — bei Gelegenheit aufräumbar, vorher kurz gegenchecken, dass kein anderer Agent gerade darin arbeitet.

## Setup Required

- GitHub-Actions-Secrets sind `DISCORD_WEBHOOK_URL`, `FIREBASE_SERVICE_ACCOUNT`, `KICKBASE_EMAIL`, `KICKBASE_LEAGUE_ID`, `KICKBASE_LEAGUE_START_BUDGET`, `KICKBASE_LEAGUE_START_DATE`, `KICKBASE_PASSWORD`. `KICKBASE_LEAGUE_START_BUDGET` steht weiterhin als Klartext-Wert direkt in beiden Workflow-YAMLs (nicht über das gleichnamige Secret referenziert) — die anderen referenzieren ihr Secret korrekt. `KICKBASE_LEAGUE_ID` neu (2026-07-31) — User hatte das Secret schon gesetzt, es wirkte aber nicht, weil es in keinem `env:`-Block referenziert war UND `select_league()` (die einzige Stelle, die es liest) nur von `fetcher.py` genutzt wurde, nicht von `dashboard_export.py`/`market_predictor.py`/`player_valuation.py` (jede Stelle pickte unabhängig ihr eigenes `leagues[0]`) — beides jetzt gefixt (`select_league()` nach `kickbase_client.py` verschoben, überall genutzt, Secret in beiden Workflow-YAMLs referenziert).

## Warnings

- **2026-08-01: Mehrere unausgeführte Pläne teilen sich `WunschkaderTab.tsx`/`App.tsx`/`formations.ts`** — der Live-Sync-Fix-Plan, der Formationen-Plan UND die parallel entstandene Wunschkader-Action-Icons-Spec ändern teils dieselben Dateien. Die eigenen Pläne dieser Session (Live-Sync-Fix, Formationen) verweisen bereits explizit aufeinander (Global Constraints/Kontext-Abschnitte), aber KEINER kennt die Action-Icons-Spec aus der parallelen Session. Vor Ausführung eines dieser Pläne: `WunschkaderTab.tsx` frisch lesen, nicht blind die "Alt:"-Codeblöcke im jeweiligen Plan-Dokument annehmen — die könnten durch einen inzwischen umgesetzten anderen Plan überholt sein.
- **2026-08-01: periodischer Drift-Check hat sich gelohnt, weiterlaufen lassen** (User-Wunsch) — mehrfach in dieser Session per `git log`/`git status`/`git worktree list` echte parallele Aktivität entdeckt (neue Commits/Worktrees von anderen Sessions), ohne dass das vorher angekündigt war. Kurzer `git log --oneline -5`/`git worktree list`-Check zu Beginn eines Tasks und vor jedem Plan-Schreiben ist billig und deckt genau das auf.
- **Immer auch die Firestore-Collection `feedback` (Dokument `feedback/current`) auf offene Einträge prüfen, nicht nur auf Chat-Anfragen reagieren** — der User hat seit 2026-07-31 einen zweiten Eingangskanal für Bugs/Ideen direkt in der App (Tab "Bugs & Ideen", `FeedbackTab.tsx`), komplett unabhängig vom Chat. `items[]` mit `status: "open"` sind unbearbeitet, `status: "done"` bereits erledigt (vom User oder einer früheren Session gesetzt). Per Sandbox-Live-Zugriff lesbar (`GOOGLE_APPLICATION_CREDENTIALS=firebase-service-account.json`), siehe [[project_kickbaseagent_sandbox_has_live_access]] falls Memory verfügbar. Dieses Feature war bis 2026-07-31 selbst in diesem HANDOFF nicht dokumentiert, obwohl es schon lief — bei jedem Session-Start aktiv nachsehen, nicht darauf verlassen, dass es hier auffällt.
- **Subagent-Ergebnisse (auch Fork-Agents) nie ungeprüft übernehmen** — siehe Failed Approaches. Immer `git diff`/`git status` prüfen, wenn ein Agent behauptet, Code geändert zu haben, bevor man das für bare Münze nimmt.
- **`npm install`/`npm run` NIE im Haupt-Checkout ausführen** (Windows-DrvFs-Mount, geteilte `node_modules` — ein `npm install` würde Unix-Bin-Shims statt `.cmd`-Dateien erzeugen und `npm run` auf der Windows/Rider-Seite brechen). In einer eigenen, isolierten Worktree ist `npm install` dagegen unproblematisch (eigene `node_modules`-Kopie, bestätigt am players-Map-Branch). `node node_modules/typescript/bin/tsc -p tsconfig.json --noEmit` ist der Standard-Verifikationsschritt sonst.
- **Kein Firestore-Doppelschreiben** — der atomare Cutover ist umgesetzt; nicht nachträglich einen Parallel-Schreibpfad einbauen.
- **Push-Policy seit 2026-07-30 geändert**: Claude darf jetzt selbst `git push` auf `main` ausführen (Ruleset-Bypass funktioniert, bestätigt) — Bedingung: ALLE Tests grün (Backend-Suite + `tsc --noEmit` falls Frontend betroffen) VOR dem Push. Kein Feature-Branch/PR-Umweg nötig. Siehe [[project_kickbaseagent_git_workflow]] falls Memory verfügbar — die alte "nur lokal committen"-Regel ist überholt.
- **Mehrere Agenten können parallel an diesem Repo arbeiten** — vor jeder größeren Aufgabe `git worktree list` prüfen.
- **Ein gesetztes GitHub-Secret alleine bewirkt nichts** — es muss zusätzlich im `env:`-Block der jeweiligen Workflow-YAML referenziert werden (`${{ secrets.NAME }}`).
- **Nach einem großen Schema-Wechsel: Frontend-Deploy und Backend-Cron laufen unabhängig** — ein Frontend-Push kann live gehen, bevor der nächste Backend-Lauf das neue Datenschema geschrieben hat. Führte hier zu einem echten White-Screen-Vorfall (siehe Completed). Nach jedem größeren Schema-Wechsel: sofort einen Heavy-Lauf erzwingen (`gh workflow run dashboard-marktwerte.yml`), nicht auf den nächsten planmäßigen Cron warten.
- **GitHub Actions liest vom Remote `main`, nicht von lokalen Commits** — vor jeder Live-Verifikation eines frischen Fixes `git log origin/main --oneline` prüfen bzw. den User fragen, ob schon gepusht wurde.
- **Plan-Dokumente können durch spätere Korrekturen veraltet werden** — bei Arbeit an einem bereits laufenden Redesign immer den aktuellen Code, nicht den ursprünglichen Plantext, als Ground Truth nehmen.
- **Status-Code-Bedeutungen nicht neu raten** — `MDs/codes.md` hat die verifizierte Zuordnung inkl. Korrektur-Historie; nur bei neuem, bisher unbeobachtetem Code (aktuell nur Code 8 offen) überhaupt neu recherchieren, und dann nur mit echtem In-App-Beleg.
