# 3-Tage-Modell: eigene Hyperparameter-Suche Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ein einmaliges Experiment-Skript (`src/experiment_target3d_tuning.py`), das per zeitbudget-gesteuerter Zufallssuche (RandomForest/HistGradientBoosting/LightGBM/XGBoost) prüft, ob das 3-Tage-Trainingsziel (`TARGET_3D`) andere Hyperparameter braucht als das bereits getunte 1-Tages-Ziel — inkl. einer echten, bisher nie gemessenen 3-Tage-Baseline.

**Architecture:** Direkte Portierung von `src/experiment_deep_tuning.py` (bereits gelöscht, letzter Stand per `git show e530b2b:src/experiment_deep_tuning.py` eingesehen), mit drei Anpassungen: `target_col`/`unclipped_col`-Parametrisierung statt hartkodiertem `TARGET`, NaN-Tail-Schutz für `TARGET_3D`s unbekannte letzte Tage pro Spieler, aktualisierter `_build_corpus()`-Aufruf (4. Argument `fitness_events_by_player` kam nach der Fitness-Historie-Feature dazu). Vor der eigentlichen Suche wird die aktuelle, geteilte `_build_candidates()`-Config als Baseline gegen `TARGET_3D` gemessen.

**Tech Stack:** Python 3.11, scikit-learn (schon in `requirements.txt`), LightGBM/XGBoost (nur in einer separaten `.venv-tuning/`, keine Produktions-Dependency). Zeitbudget per `TUNING_BUDGET_HOURS`-Env-Var.

## Global Constraints

- Keine Änderung an `src/market_predictor.py` (Produktionscode) — das Experiment-Skript ist komplett eigenständig, importiert nur bestehende, unveränderte Funktionen/Konstanten daraus.
- Keine neue Produktions-Dependency — LightGBM/XGBoost ausschließlich in `.venv-tuning/` (bereits in `.gitignore`), `requirements.txt` bleibt unangetastet.
- 30 Folds (nicht `BACKTEST_FOLDS=6` aus der Produktions-Konstante übernehmen — das ist bewusst die schnelle Live-Kachel-Metrik, keine belastbare Tuning-Grundlage; die letzte Session fand mit nur 6 Folds ein falsch-negatives Ergebnis).
- Baseline-Vergleichskriterium (identisch zum bereits erfolgreichen 1-Tages-Tuning): eine Konfiguration gilt nur als "besser", wenn Sign-Accuracy ≥ Baseline+1pt UND MAE ≤ Baseline.
- Skript ist ein einmaliges Experiment (analog `experiment_deep_tuning.py`/`experiment_hgb_tuning.py`/etc.) — nach dem letzten tatsächlichen Lauf wieder löschen, nicht dauerhaft im Repo behalten.

---

## Datei-Übersicht

| Datei | Rolle |
|---|---|
| `src/experiment_target3d_tuning.py` | NEU — das komplette Experiment (Baseline-Messung + zeitbudget-gesteuerte Zufallssuche über 4 Modellfamilien) |
| `.venv-tuning/` | NEU (lokal, nicht committed) — separates venv mit LightGBM/XGBoost zusätzlich zu `requirements.txt` |

---

## Task 1: Experiment-Skript erstellen + Smoke-Test

**Files:**
- Create: `src/experiment_target3d_tuning.py`

**Interfaces:**
- Consumes: `market_predictor.py`s `TARGET_3D`, `FEATURES`, `RANDOM_STATE`, `BACKTEST_MIN_TRAIN_ROWS`, `_build_candidates()`, `_build_corpus()`, `_engineer_features()`, `_load_fitness_events_by_player()` (alle bereits vorhanden, unverändert) sowie `kickbase_client.py`s `login()`/`get_me()`.
- Produces: eigenständiges CLI-Skript, keine von anderem Code importierten Symbole.

Kein Unittest-Suite für dieses Skript — etabliertes Muster für alle `experiment_*.py`-Dateien dieses Repos (einmalige Experimente, kein Produktionscode). Verifikation stattdessen über einen echten, winzigen Smoke-Lauf (Step 3) gegen die Sandbox' echten Kickbase-Zugang.

- [ ] **Step 1: `.venv-tuning` anlegen**

```bash
python3 -m venv .venv-tuning
.venv-tuning/bin/pip install -r requirements.txt
.venv-tuning/bin/pip install lightgbm xgboost
```

Expected: läuft durch, keine Fehler. (`.venv-tuning/` ist bereits in `.gitignore` — kein versehentlicher Commit.)

- [ ] **Step 2: Skript schreiben**

Neue Datei `src/experiment_target3d_tuning.py`:

```python
"""Einmaliges Experiment-Skript (nach dem letzten tatsaechlichen Lauf
wieder loeschen, analog experiment_deep_tuning.py/experiment_hgb_tuning.py/
etc.): randomisierte Hyperparameter-Suche fuer das 3-Tage-Trainingsziel
(TARGET_3D/mv_target_3d_clipped) - die "Nacht-Runde" (277 Konfigs, 9h) fand
eine verbesserte HistGradientBoosting-Config nur gegen das 1-Tages-Ziel
(TARGET). Ob TARGET_3D dieselben Parameter optimal nutzt, wurde nie
separat gemessen.

Portiert aus experiment_deep_tuning.py (git show e530b2b:src/
experiment_deep_tuning.py), mit drei Anpassungen: target_col/
unclipped_col statt hartkodiertem TARGET/"mv_target", NaN-Tail-Schutz
(TARGET_3D hat pro Spieler ein paar Tage am Ende ohne bekannten Ausgang -
train/test werden deshalb separat auf ihre jeweils relevante Spalte hin
von NaN befreit, exakt wie in market_predictor.py::_walk_forward_backtest()
bereits einmal geloest, siehe dessen Docstring), und der um
fitness_events_by_player erweiterte _build_corpus()-Aufruf.

Zeitbudget-gesteuert (TUNING_BUDGET_HOURS-Env-Var, Default 2) - GitHub
Actions hat ein hartes 6h-Job-Limit auf gehosteten Runnern, das Skript
hoert deshalb rechtzeitig VOR dem Budget auf (Sicherheitsmarge = letzte
Konfigurationsdauer * 1.3). Vor der eigentlichen Suche wird zuerst die
AKTUELLE, geteilte _build_candidates()-Config gegen TARGET_3D gemessen
("BASELINE") - der Vergleichsmassstab fuer jede gefundene Konfiguration.

Konfigurationen werden per sklearn ParameterSampler zufaellig aus einem
Bereich pro Familie gezogen, Round-Robin ueber die 4 Familien verteilt,
damit bei vorzeitigem Zeit-Ende keine Familie systematisch benachteiligt
wird. Nach JEDER fertigen Konfiguration wird die aktuelle Bestenliste neu
ausgegeben - bei einem Absturz/Abbruch bleibt trotzdem ein verwertbares
Zwischenergebnis im Log."""

import os
import sys
import time

import numpy as np
from dotenv import load_dotenv
from sklearn.ensemble import HistGradientBoostingRegressor, RandomForestRegressor
from sklearn.model_selection import ParameterSampler

from src.kickbase_client import get_me, login
from src.market_predictor import (
    BACKTEST_MIN_TRAIN_ROWS,
    FEATURES,
    RANDOM_STATE,
    TARGET_3D,
    _build_candidates,
    _build_corpus,
    _engineer_features,
    _load_fitness_events_by_player,
)

FOLDS = 30
N_SAMPLES_PER_FAMILY = 100  # obere Schranke, Zeitbudget bricht i.d.R. frueher ab
TARGET_COL = TARGET_3D
UNCLIPPED_COL = TARGET_COL.removesuffix("_clipped")

PARAM_SPACES = {
    "RandomForest": {
        "n_estimators": [200, 500, 800],
        "max_depth": [10, 20, 30, None],
        "min_samples_split": [2, 5, 10],
        "min_samples_leaf": [1, 2, 4],
        "max_features": ["sqrt", "log2", 0.5],
    },
    "HistGradientBoosting": {
        "learning_rate": [0.02, 0.05, 0.1, 0.2],
        "max_iter": [100, 200, 300, 500],
        "max_leaf_nodes": [15, 31, 63, 127],
        "l2_regularization": [0.0, 0.1, 1.0],
        "min_samples_leaf": [10, 20, 50],
    },
    "LightGBM": {
        "n_estimators": [100, 300, 500],
        "learning_rate": [0.02, 0.05, 0.1, 0.2],
        "num_leaves": [15, 31, 63, 127],
        "min_child_samples": [10, 20, 50],
        "subsample": [0.7, 0.85, 1.0],
    },
    "XGBoost": {
        "n_estimators": [100, 300, 500],
        "learning_rate": [0.02, 0.05, 0.1, 0.2],
        "max_depth": [3, 6, 9],
        "subsample": [0.7, 0.85, 1.0],
        "colsample_bytree": [0.7, 0.85, 1.0],
    },
}


def _build_model(family: str, params: dict):
    if family == "RandomForest":
        return RandomForestRegressor(n_jobs=-1, random_state=RANDOM_STATE, **params)
    if family == "HistGradientBoosting":
        return HistGradientBoostingRegressor(random_state=RANDOM_STATE, **params)
    if family == "LightGBM":
        from lightgbm import LGBMRegressor

        return LGBMRegressor(n_jobs=-1, random_state=RANDOM_STATE, verbosity=-1, **params)
    if family == "XGBoost":
        from xgboost import XGBRegressor

        return XGBRegressor(n_jobs=-1, random_state=RANDOM_STATE, objective="reg:squarederror", **params)
    raise ValueError(family)


def _build_candidate_queue():
    """Round-Robin ueber die 4 Familien, damit bei vorzeitigem Zeit-Ende
    keine Familie systematisch benachteiligt wird."""
    samplers = {
        family: iter(ParameterSampler(space, n_iter=N_SAMPLES_PER_FAMILY, random_state=RANDOM_STATE))
        for family, space in PARAM_SPACES.items()
    }
    queue = []
    families = list(PARAM_SPACES)
    exhausted = set()
    while len(exhausted) < len(families):
        for family in families:
            if family in exhausted:
                continue
            try:
                queue.append((family, next(samplers[family])))
            except StopIteration:
                exhausted.add(family)
    return queue


def _fold_cutoffs(history_df):
    dates = sorted(history_df["date"].unique())
    if len(dates) <= FOLDS:
        raise RuntimeError(f"Zu wenig Tage fuer {FOLDS} Folds: {len(dates)}")
    return dates[-FOLDS:]


def _evaluate_model(history_df, cutoffs, model_factory) -> dict | None:
    """Fuehrt EIN Modell durch alle Cutoffs - model_factory() wird PRO FOLD
    neu aufgerufen und muss eine frische, ungefittete Instanz liefern
    (exakt wie market_predictor.py::_walk_forward_backtest(), das
    _build_candidates() ebenfalls innerhalb der Fold-Schleife neu aufruft -
    kein wiederverwendetes, ueber Folds hinweg mutiertes Objekt). Fuer
    TARGET_3D notwendig: train UND test werden separat auf ihre jeweils
    relevante Spalte hin von NaN befreit (die letzten paar Tage pro
    Spieler kennen ihren 3-Tage-Ausgang noch nicht) - exakt dieselbe Falle,
    die in _walk_forward_backtest() bereits einmal gefunden und gefixt
    wurde (siehe dessen Docstring)."""
    sign_hits: list[bool] = []
    abs_errors: list[float] = []
    per_fold_accuracy: list[float] = []
    folds_run = 0

    for cutoff in cutoffs:
        train = history_df[history_df["date"] < cutoff].dropna(subset=[TARGET_COL])
        test = history_df[history_df["date"] == cutoff].dropna(subset=[UNCLIPPED_COL])
        if len(train) < BACKTEST_MIN_TRAIN_ROWS or test.empty:
            continue
        folds_run += 1

        x_train, y_train = train[FEATURES].astype(float), train[TARGET_COL]
        x_test = test[FEATURES].astype(float)
        y_test_actual = test[UNCLIPPED_COL]

        model = model_factory()
        model.fit(x_train, y_train)
        y_pred = model.predict(x_test)
        hits = (np.sign(y_test_actual) == np.sign(y_pred)).tolist()
        errors = np.abs(y_test_actual - y_pred).tolist()
        sign_hits.extend(hits)
        abs_errors.extend(errors)
        per_fold_accuracy.append(float(np.mean(hits)) * 100 if hits else None)

    if not sign_hits:
        return None

    fold_accs = [a for a in per_fold_accuracy if a is not None]
    return {
        "sign_accuracy": round(float(np.mean(sign_hits)) * 100, 1),
        "mae": round(float(np.mean(abs_errors)), 0),
        "fold_std": round(float(np.std(fold_accs)), 1) if len(fold_accs) > 1 else 0.0,
        "n": len(sign_hits),
        "folds_run": folds_run,
    }


def _evaluate_config(history_df, cutoffs, family: str, params: dict) -> dict:
    result = _evaluate_model(history_df, cutoffs, lambda: _build_model(family, params))
    if result is None:
        raise RuntimeError("Kein Fold hatte genug Trainingsdaten")
    return {"family": family, "params": params, **result}


def _print_leaderboard(results: list[dict]) -> None:
    # Nach MAE sortiert, NICHT nach Accuracy - Configs mit Unterfitting
    # trafen im letzten Durchlauf oefter zufaellig die Richtung, lagen
    # dabei aber im Betrag katastrophal daneben.
    ranked = sorted(results, key=lambda r: r["mae"])
    print(f"\n--- Zwischenstand ({len(results)} Konfigurationen fertig, nach MAE sortiert) ---")
    print(f"{'Familie':<20} {'Accuracy':>10} {'MAE':>10} {'Fold-Std':>10} {'Params'}")
    for r in ranked[:10]:
        print(f"{r['family']:<20} {r['sign_accuracy']:>9.1f}% {r['mae']:>10.0f} {r['fold_std']:>9.1f}p {r['params']}")
    print("---\n", file=sys.stderr)


def run(history_df, budget_seconds: float) -> None:
    cutoffs = _fold_cutoffs(history_df)

    print("Messe Baseline (aktuelle _build_candidates()-Config gegen TARGET_3D) ...", file=sys.stderr)
    for name in _build_candidates():
        baseline = _evaluate_model(history_df, cutoffs, lambda n=name: _build_candidates()[n])
        if baseline is None:
            print(f"BASELINE {name}: kein Fold hatte genug Daten", file=sys.stderr)
            continue
        print(
            f"BASELINE {name:<20} {baseline['sign_accuracy']:>5.1f}% "
            f"MAE={baseline['mae']:.0f} std={baseline['fold_std']:.1f}p (n={baseline['n']}, folds={baseline['folds_run']})"
        )

    queue = _build_candidate_queue()
    print(f"{len(queue)} Konfigurationen in der Warteschlange, Zeitbudget {budget_seconds/3600:.1f}h", file=sys.stderr)

    results = []
    start = time.monotonic()
    last_config_duration = 0.0

    for i, (family, params) in enumerate(queue):
        elapsed = time.monotonic() - start
        remaining = budget_seconds - elapsed
        if last_config_duration and remaining < last_config_duration * 1.3:
            print(
                f"Zeitbudget bald erreicht ({elapsed/60:.1f}min verstrichen, {remaining/60:.1f}min uebrig, "
                f"letzte Konfig brauchte {last_config_duration/60:.1f}min) - Suche wird jetzt beendet.",
                file=sys.stderr,
            )
            break

        config_start = time.monotonic()
        print(f"[{i + 1}/{len(queue)}] {family} {params} ...", file=sys.stderr)
        try:
            result = _evaluate_config(history_df, cutoffs, family, params)
            results.append(result)
        except Exception as exc:
            print(f"Warnung: Konfiguration fehlgeschlagen, uebersprungen: {exc}", file=sys.stderr)
            continue
        last_config_duration = time.monotonic() - config_start

        print(
            f"ERGEBNIS {result['family']:<20} {result['sign_accuracy']:>5.1f}% "
            f"MAE={result['mae']:.0f} std={result['fold_std']:.1f}p {result['params']}"
        )

        if len(results) % 3 == 0 or i == len(queue) - 1:
            _print_leaderboard(results)

    print(f"\n=== ENDERGEBNIS: {len(results)} Konfigurationen getestet in {(time.monotonic() - start)/3600:.2f}h ===\n")
    _print_leaderboard(results)


if __name__ == "__main__":
    load_dotenv()
    email = os.environ.get("KICKBASE_EMAIL")
    password = os.environ.get("KICKBASE_PASSWORD")
    if not email or not password:
        raise RuntimeError("KICKBASE_EMAIL/KICKBASE_PASSWORD fehlen (lokal: .env, GitHub Actions: Secrets)")
    budget_hours = float(os.environ.get("TUNING_BUDGET_HOURS", "2"))

    token, _user, leagues = login(email, password)
    league_id = leagues[0]["id"]
    competition_id = get_me(token, league_id).get("cpi") or "1"

    print("Baue Corpus (einmalig)...", file=sys.stderr)
    fitness_events_by_player = _load_fitness_events_by_player()
    corpus = _build_corpus(token, league_id, competition_id, fitness_events_by_player)
    history_df, _today_df = _engineer_features(corpus)
    print(f"{len(history_df)} Trainings-Zeilen im Corpus", file=sys.stderr)

    run(history_df, budget_seconds=budget_hours * 3600)
```

- [ ] **Step 3: Winziger Smoke-Test (echte Sandbox-Kickbase-Verbindung, kein Mock)**

Run (aus Repo-Root, mit `.venv-tuning`):

```bash
TUNING_BUDGET_HOURS=0.03 .venv-tuning/bin/python -m src.experiment_target3d_tuning
```

Expected:
- `Baue Corpus (einmalig)...` gefolgt von `N Trainings-Zeilen im Corpus` (N > 0, kein Crash beim Login/Corpus-Aufbau).
- Mindestens eine `BASELINE`-Zeile pro Kandidat (`RandomForest`, `HistGradientBoosting`) — **kein `NaN` in `MAE=`/Accuracy-Werten** (das wäre das Zeichen, dass der NaN-Tail-Schutz fehlt oder falsch ist).
- Mindestens eine `ERGEBNIS`-Zeile aus der Suche, ebenfalls ohne `NaN`.
- Skript endet mit `=== ENDERGEBNIS: ... ===` und einer Bestenliste (auch wenn nur 1-2 Konfigurationen bei diesem winzigen Budget fertig wurden).

Falls `NaN` auftaucht: `_evaluate_model()`s beide `dropna(subset=[...])`-Zeilen prüfen — häufigster Fehler ist, eine davon zu vergessen oder auf der falschen Spalte (`TARGET_COL` vs. `UNCLIPPED_COL`) anzuwenden.

- [ ] **Step 4: Commit**

```bash
git add src/experiment_target3d_tuning.py
git commit -m "experiment_target3d_tuning: eigene Hyperparameter-Suche fuer TARGET_3D (Baseline + randomisierte Suche RF/HGB/LightGBM/XGBoost)"
```

---

## Nach diesem Plan (keine Tasks — operative Schritte für eine spätere Session)

- [ ] **Kurzer Check**: `TUNING_BUDGET_HOURS=1.5 .venv-tuning/bin/python -m src.experiment_target3d_tuning`. Zeigt schnell, ob überhaupt eine Konfiguration die Baseline schlägt.
- [ ] **Tiefer Lauf** (nur falls der kurze Check eine vielversprechende Richtung zeigt, oder direkt gewünscht): `TUNING_BUDGET_HOURS=9 .venv-tuning/bin/python -m src.experiment_target3d_tuning`, am besten mit `nohup ... &` oder `tmux`/`screen`, da 9h — Ausgabe optional nach `tuning-results/target3d.log` umleiten (Verzeichnis schon in `.gitignore`).
- [ ] **Falls ein Gewinner gefunden wird** (Sign-Accuracy ≥ Baseline+1pt UND MAE ≤ Baseline, siehe Global Constraints): eigener Folge-Plan — `_build_candidates()` bekäme eine horizon-abhängige Signatur, inkl. Anpassung der Aufrufstellen in `_train_and_evaluate()`/`_walk_forward_backtest()`/Tests. Nicht Teil dieses Plans.
- [ ] **Falls kein Gewinner gefunden wird**: Ergebnis in `HANDOFF.md` dokumentieren (analog zu den bisherigen "Failed Approaches"-Einträgen).
- [ ] **Skript löschen** nach dem letzten tatsächlichen Lauf (`git rm src/experiment_target3d_tuning.py`), etabliertes Muster für alle `experiment_*.py`-Dateien dieses Repos.

## Self-Review-Notiz (bereits durchgeführt)

- **Spec-Abdeckung**: alle Punkte aus der Spec (ein Skript, zwei Budgets, generalisierte Fold-Auswertung ohne Produktionscode-Änderung, Baseline-zuerst, `.venv-tuning`-Setup, bedingter Folge-Schritt) sind in Task 1 bzw. im "Nach diesem Plan"-Abschnitt abgedeckt.
- **Platzhalter-Scan**: keine TBD/"analog zu"-ohne-Code gefunden — der komplette Skript-Code ist ausgeschrieben, keine Verweise auf nicht existierenden Code.
- **Typ-Konsistenz**: `TARGET_COL`/`UNCLIPPED_COL`/`_evaluate_model()`/`_evaluate_config()`/`_build_candidate_queue()` durchgängig gleich benannt, keine Abweichung zwischen Docstring-Beschreibung und tatsächlichem Code.
- **Gegen den echten Code verifiziert**: `src/market_predictor.py` wurde vollständig gelesen (aktueller main-Stand) für `_build_candidates()`, `_walk_forward_backtest()` (Referenz für den NaN-Tail-Fix UND das Pro-Fold-Neuaufbau-Muster, das die Baseline-Messung hier bewusst repliziert), `_build_corpus()`s aktuelle 4-Parameter-Signatur, `TARGET_3D`, `BACKTEST_MIN_TRAIN_ROWS`. Das alte `experiment_deep_tuning.py` wurde per `git show e530b2b:src/experiment_deep_tuning.py` im Volltext eingesehen (nicht aus Erinnerung rekonstruiert) — die drei Anpassungen sind die einzigen inhaltlichen Unterschiede zum Original. `requirements.txt` wurde für die exakten Versionen im `.venv-tuning`-Setup-Schritt gelesen.
